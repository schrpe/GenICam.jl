var documenterSearchIndex = {"docs":
[{"category":"section","location":"streaming/#Streaming","page":"Streaming","text":"grab does single-frame acquisition: configure → trigger → wait → return. For live preview, frame-rate measurement, or any longer-running session, use the streaming API: a background Task that pushes decoded frames into a Channel as the camera produces them.","title":"Streaming"},{"category":"section","location":"streaming/#Two-entry-points","page":"Streaming","text":"# do-block — recommended for scoped use\nstream(cam) do channel\n    for frame in Iterators.take(channel, 100)\n        process(frame.image)\n    end\nend\n# stop_stream is called automatically on exit (success or exception)\n\n# explicit — when the stream needs to outlive a single block\nsh = start_stream(cam; num_buffers = 8)\nfor _ in 1:100\n    frame = take!(sh.channel)\n    process(frame.image)\nend\nstop_stream(cam)\n\nstream without a do returns the channel directly; you're responsible for stop_stream.","title":"Two entry points"},{"category":"section","location":"streaming/#Configuration","page":"Streaming","text":"start_stream(cam;\n    num_buffers   = 8,             # producer-side buffer pool size\n    decode        = true,           # push DecodedFrame (true) or Vector{UInt8} (false)\n    channel_size  = 4,              # Julia Channel capacity\n    policy        = DROP_OLDEST,    # what to do when channel is full\n    timeout_ms    = 1000,           # per-frame fetch timeout\n)\n\nnum_buffers — how many image buffers the producer can fill ahead of the consumer. Larger values absorb consumer hiccups without dropping frames; the cost is RAM (each buffer is full payload-size).\ndecode — when true (default) each frame is run through the pixel-format decoder and the channel yields DecodedFrame values. When false, raw producer bytes are copied into a fresh Vector{UInt8} and pushed; useful when you want to ship bytes elsewhere (network, file) without paying for a decode you'll throw away.\nchannel_size — controls how many frames sit between producer and consumer. Combined with policy it determines what happens when consumer is slower.","title":"Configuration"},{"category":"section","location":"streaming/#Backpressure-policies","page":"Streaming","text":"Policy When channel is full Best for\nDROP_OLDEST (default) drop the oldest queued frame, push the new one live preview — freshness matters more than completeness\nDROP_NEWEST discard the newly arrived frame rate-limited storage where order matters\nBLOCK block the producer task until consumer drains offline analysis where every frame must be kept\n\nDROP_OLDEST and DROP_NEWEST increment frames_dropped when they drop. BLOCK will eventually stall the camera (the producer's buffer pool fills up); use only when you must not lose frames and your consumer can keep up on average.","title":"Backpressure policies"},{"category":"section","location":"streaming/#Statistics","page":"Streaming","text":"sh = start_stream(cam)\n# ... consumer loop ...\nstop_stream(cam)\n\nframes_grabbed(cam)       # how many frames actually ended up on the channel\nframes_dropped(cam)       # how many were dropped due to backpressure (or decode failure)\nis_streaming(cam)          # true while the stream task is alive\n\nThe counters survive stop_stream — call them after the stream ends to see the final tally. Starting a new stream resets them (each start_stream creates a fresh StreamHandle).","title":"Statistics"},{"category":"section","location":"streaming/#Lifecycle-and-cleanup","page":"Streaming","text":"A few invariants worth knowing:\n\nOnly one stream may be active per camera at a time — start_stream throws if another is running.\nIf you've used grab before streaming, the single-frame buffer pool is torn down automatically when start_stream runs (only one EVENT_NEW_BUFFER registration is allowed per data stream).\nclose(cam) calls stop_stream first — the camera object is the canonical owner.\nThe streaming task always re-queues each buffer to the producer before pushing to the channel, so the producer never starves waiting for a buffer the consumer hasn't drained yet.","title":"Lifecycle and cleanup"},{"category":"section","location":"streaming/#GC-safety","page":"Streaming","text":"When decode = true the decoder allocates a fresh image, so the channel value owns its memory. Safe.\n\nWhen decode = false the streaming task copies the producer's bytes into a fresh Vector{UInt8} before pushing. Also safe — but if you're tempted to skip the copy (for performance), don't: the underlying producer buffer is re-queued immediately and the next frame overwrites it. The copy is essential.","title":"GC safety"},{"category":"section","location":"streaming/#Example:-rolling-FPS","page":"Streaming","text":"function fps_for(cam, seconds = 5.0)\n    t0 = time()\n    n = 0\n    stream(cam; num_buffers = 8) do channel\n        while time() - t0 < seconds\n            isready(channel) || (sleep(0.01); continue)\n            take!(channel)\n            n += 1\n        end\n    end\n    return n / (time() - t0)\nend\n\nset_aoi!(cam; width = 640, height = 480)\ncam.PixelFormat = \"Mono8\"\n@info \"achieved fps\" fps = fps_for(cam, 5)","title":"Example: rolling FPS"},{"category":"section","location":"streaming/#Example:-ring-buffer","page":"Streaming","text":"using GenICam\n\nmutable struct Ring{T}\n    buf::Vector{T}\n    pos::Int\n    n::Int\nend\nRing{T}(cap::Int) where {T} = Ring{T}(Vector{T}(undef, cap), 0, 0)\nfunction push_!(r::Ring{T}, x::T) where {T}\n    r.pos = mod1(r.pos + 1, length(r.buf))\n    r.buf[r.pos] = x\n    r.n += 1\nend\n\nring = Ring{Any}(60)        # last 60 frames\nsh = start_stream(cam)\n@async begin\n    for frame in sh.channel\n        push_!(ring, frame)\n    end\nend\n\n# ... main thread does whatever ...\n\n# At any point you can inspect the most-recent N frames:\nlast_frames = [ring.buf[mod1(ring.pos - i, length(ring.buf))] for i in 0:9]\nstop_stream(cam)\n\nThe BLOCK policy is preferable here because we never want to drop frames into a ring buffer (drops would silently produce gaps). With DROP_OLDEST you'd see decreasing fidelity if the consumer ever stalls.\n\nSee API reference for full signatures of stream, start_stream, stop_stream, is_streaming, frames_grabbed, frames_dropped, StreamPolicy, and StreamHandle.","title":"Example: ring buffer"},{"category":"section","location":"concepts/#Background:-the-GenICam-standard-family","page":"Concepts","text":"This page explains the concepts GenICam.jl builds on — enough to make sense of the rest of the documentation. It's deliberately filtered to what matters for using this library; the full EMVA specifications (GenTL 1.5, GenApi 1.1, SFNC 2.7, PFNC 2.4) are 200+ PDF pages each.","title":"Background: the GenICam standard family"},{"category":"section","location":"concepts/#Why-GenICam-exists","page":"Concepts","text":"Industrial cameras predate USB cameras by decades. By the early 2000s every vendor had their own SDK, their own register map, their own way of saying \"set the exposure time\". Mixing two cameras in one application meant mixing two SDKs.\n\nThe European Machine Vision Association (EMVA) hammered this out into a four-piece standard so that any camera and any application can talk to each other:\n\nGenTL — a stable C ABI for transport-layer access (USB3 Vision, GigE Vision, Camera Link, CoaXPress, ...). Vendors ship a \"GenTL producer\" DLL; applications are \"GenTL consumers\". The producer knows the wire protocol; the consumer doesn't have to.\nGenApi — an XML node-map embedded in every camera that describes its features (Width, ExposureTime, TriggerMode, ...) and how each maps to a register or a computed expression.\nSFNC — the Standard Features Naming Convention. So every vendor's camera exposes \"Width\" with the same name, the same units, the same semantics.\nPFNC — the Pixel Format Naming Convention. A registry of 32-bit codes that buffers carry to identify their pixel layout.\n\nGenICam.jl is a pure-Julia consumer of all four pieces. You bring a producer DLL (any vendor); the package handles the rest.","title":"Why GenICam exists"},{"category":"section","location":"concepts/#Producer-and-consumer","page":"Concepts","text":"┌──────────────────────────────────────────────────────────┐\n│                                                          │\n│   Application                                            │\n│   ┌──────────────────────┐                               │\n│   │  your Julia code     │                               │\n│   └──────────┬───────────┘                               │\n│              │ uses                                       │\n│   ┌──────────▼───────────┐                               │\n│   │   GenICam.jl         │  ← consumer                   │\n│   │  (this package)      │                               │\n│   └──────────┬───────────┘                               │\n│              │ ccall via Libdl                            │\n└──────────────┼──────────────────────────────────────────┘\n               │\n┌──────────────▼──────────────────────────────────────────┐\n│   Vendor producer DLL  (.cti file)                      │\n│   ┌────────────────────────────────────────┐             │\n│   │  Implements the GenTL_v1_5.h ABI       │  ← producer │\n│   │  Knows the wire protocol               │             │\n│   └────────────────────────────────────────┘             │\n└──────────────┬──────────────────────────────────────────┘\n               │\n               │ wire protocol (GigE Vision / U3V / CXP / ...)\n               ▼\n       ┌─────────────────┐\n       │     Camera      │\n       └─────────────────┘\n\nThe producer is a regular shared library (.dll / .so / .dylib) renamed to .cti by convention. GenICam.jl dlopens it and resolves its exported symbols (GCInitLib, TLOpen, IFOpenDevice, ...) via Libdl.dlsym into a function-pointer table — every subsequent ccall goes through that table.\n\nYou don't pick which producer at compile time. You can load multiple producers in one Julia process — list_all_cameras() does exactly that and returns devices tagged with the producer that found them.","title":"Producer and consumer"},{"category":"section","location":"concepts/#Handle-hierarchy","page":"Concepts","text":"GenTL is a handle-based C API. Every operation takes an opaque pointer (\"handle\") that names some module of the producer. The handles nest:\n\nProducerAPI                  (one per .cti — the dlopen handle + symbol table)\n  │\n  ▼\nProducer / TL_HANDLE         (the \"Transport Layer\" — TLOpen result)\n  │\n  ▼\nInterface / IF_HANDLE        (one per detected bus / NIC / PCIe slot)\n  │\n  ▼\nDevice / DEV_HANDLE          (one per connected camera)\n  │  ├─ Port / PORT_HANDLE   (the camera's register map; DevGetPort)\n  │  └─ Remote Device        (alias for the camera-side feature space)\n  │\n  ▼\nDataStream / DS_HANDLE       (an image-stream channel — usually one per camera)\n  │\n  ▼\nBuffer / BUFFER_HANDLE       (one per announced image buffer in the pool)\n  │\n  ▼\nEvent / EVENT_HANDLE         (registered notifications — new-buffer, feature-change, ...)\n\nEach level knows its parent (IFGetParentTL, DevGetParentIF, DSGetParentDev) so you can walk back up. GenICam.jl represents each as a Julia struct that owns its handle and runs the matching close on Base.close or finalisation: GenTL.Producer, GenTL.Interface, GenTL.Device, GenTL.DataStream, GenTL.Acquisition.","title":"Handle hierarchy"},{"category":"section","location":"concepts/#GenTL-ABI-conventions","page":"Concepts","text":"The whole API is built on a few patterns worth knowing.","title":"GenTL ABI conventions"},{"category":"section","location":"concepts/#Error-codes","page":"Concepts","text":"Every function returns GC_ERROR (Int32). 0 (GC_ERR_SUCCESS) means success; everything else is a failure code (GC_ERR_TIMEOUT, GC_ERR_INVALID_HANDLE, GC_ERR_NOT_AVAILABLE, ...). The full list is in GenTL_v1_5.h and mirrored as Julia constants in GenTL.types.jl.\n\nAfter a non-zero return you can call GCGetLastError for a human-readable message. GenICam.jl does this automatically and wraps every error in a GenTL.GenTLError exception with both the numeric code and the producer's last-error string.","title":"Error codes"},{"category":"section","location":"concepts/#Two-pass-size-query-pattern","page":"Concepts","text":"Functions that return strings or variable-length data take a size_t *piSize argument. Callers invoke them twice:\n\nWith pBuffer = NULL and *piSize = 0 to query the required size.\nWith a buffer of the returned size to actually fetch the data.\n\nThe Julia wrappers hide this; you call e.g. GenTL.tl_get_info_string and get the string back.","title":"Two-pass size-query pattern"},{"category":"section","location":"concepts/#Modules-and-\"info\"-commands","page":"Concepts","text":"Most module types (TL, IF, Device, DataStream, Buffer, Port) have a *GetInfo function plus an enum of INFO_CMD values:\n\nproducer_info(p, TL_INFO_VENDOR)        # \"Balluff\"\nproducer_info(p, TL_INFO_MODEL)         # \"Balluff GenTL Producer...\"\ndevice_info(d, DEVICE_INFO_VENDOR)      # \"MATRIX VISION GmbH\"\ndevice_info(d, DEVICE_INFO_MODEL)       # \"mvBlueFOX3-1013C\"\nds_get_info(api, ds, STREAM_INFO_PAYLOAD_SIZE, Csize_t)   # 614400\n\nThis is the standard introspection mechanism — there's no getDeviceVendor() function, just IFGetDeviceInfo(..., DEVICE_INFO_VENDOR, ...).","title":"Modules and \"info\" commands"},{"category":"section","location":"concepts/#Buffer-model","page":"Concepts","text":"Image buffers in GenTL are consumer-allocated. The flow:\n\nConsumer (GenICam.jl) allocates N Julia Vector{UInt8} of the payload size.\nFor each buffer: DSAnnounceBuffer(ds, ptr, size, private, &handle) — the producer now knows about this memory region.\nDSQueueBuffer(ds, handle) — the buffer is now in the producer's \"input pool\" waiting to be filled.\nDSStartAcquisition(ds, ...) — the producer begins filling buffers.\nThe consumer registers an EVENT_NEW_BUFFER event and polls EventGetData to learn when each buffer is full.\nConsumer reads the data, calls DSQueueBuffer again to recycle.\nOn stop: DSStopAcquisition then DSRevokeBuffer for each.\n\nThe fact that the consumer owns the memory is what makes pure-Julia acquisition possible — the producer writes pixels directly into our Vector{UInt8} and we read them back as a typed matrix in Julia.","title":"Buffer model"},{"category":"section","location":"concepts/#Event-model","page":"Concepts","text":"Events are registered on a \"source\" handle (data stream, device, port, ...) and identified by an EVENT_TYPE:\n\nEVENT_NEW_BUFFER          — new image filled (per data stream)\nEVENT_FEATURE_INVALIDATE  — camera says a feature value is now stale\nEVENT_FEATURE_CHANGE      — camera pushes a new feature value\nEVENT_REMOTE_DEVICE       — generic remote-device event\nEVENT_ERROR               — module-level error notification\n\nGCRegisterEvent returns an EVENT_HANDLE; EventGetData(h, ..., timeout) blocks for the next event of that type. GenICam.jl wraps these:\n\nBuffer events: handled internally by GenTL.Acquisition.\nFeature events: exposed via on_feature_invalidate and on_feature_change with graceful no-op for producers that don't support them.","title":"Event model"},{"category":"section","location":"concepts/#GenApi-node-map","page":"Concepts","text":"The producer exposes the camera through DevGetPort — a port-handle into the camera's register space. Through that port, two things flow:\n\nRegister reads/writes — bytes addressed by 64-bit offsets.\nA bundled XML document — the camera's GenApi node-map, typically ZIP-compressed and 200–1000 KB.\n\nGenICam.jl downloads the XML on open_camera and parses it into a graph of nodes:\n\nRegisterDescription (root)\n│\n├── <Category Name=\"Root\">\n│   ├── <pFeature>DeviceControl</pFeature>\n│   ├── <pFeature>ImageFormatControl</pFeature>\n│   └── <pFeature>AcquisitionControl</pFeature>\n│\n├── <Category Name=\"ImageFormatControl\">\n│   ├── <pFeature>Width</pFeature>\n│   ├── <pFeature>Height</pFeature>\n│   └── <pFeature>PixelFormat</pFeature>\n│\n├── <Integer Name=\"Width\">         ← user-facing feature\n│   ├── <pValue>WidthReg</pValue>   ← reference to the backing register\n│   ├── <Min>1</Min>\n│   └── <Max>1920</Max>\n│\n├── <IntReg Name=\"WidthReg\">         ← the actual register\n│   ├── <Address>0xA004</Address>\n│   ├── <Length>4</Length>\n│   ├── <Sign>Unsigned</Sign>\n│   └── <Endianess>LittleEndian</Endianess>\n│\n├── <Enumeration Name=\"PixelFormat\">\n│   ├── <pValue>PixelFormatReg</pValue>\n│   ├── <EnumEntry Name=\"Mono8\"><Value>0x01080001</Value></EnumEntry>\n│   └── <EnumEntry Name=\"BayerRG8\"><Value>0x01080009</Value></EnumEntry>\n│\n├── <Command Name=\"AcquisitionStart\">\n│   ├── <pValue>AcquisitionStartReg</pValue>\n│   └── <CommandValue>1</CommandValue>\n│\n├── <SwissKnife Name=\"MaxBufferCount\">\n│   ├── <pVariable Name=\"ImgSize\">PayloadSize</pVariable>\n│   └── <Formula>(64*1024*1024) / ImgSize</Formula>\n│\n└── <Converter Name=\"ExposureTime\">     ← bidirectional formula\n    ├── <pValue>ExposureTimeReg</pValue>\n    ├── <FormulaTo>TO * 100</FormulaTo>     ← microseconds → ticks\n    └── <FormulaFrom>FROM / 100</FormulaFrom> ← ticks → microseconds","title":"GenApi node-map"},{"category":"section","location":"concepts/#Reference-types","page":"Concepts","text":"GenApi nodes form a directed graph through reference elements. The ones GenICam.jl understands:\n\nElement Direction Meaning\n<pValue>X</pValue> this node → X \"my value lives in node X\" (chain to backing register)\n<pAddress>X</pAddress> this node → X \"add X's current value to my register address\"\n<pIndex Offset=\"N\">X</pIndex> this node → X \"add X's current value × N to my register address\"\n<pInvalidator>X</pInvalidator> X → this node \"when X is written, my cached value is stale\"\n<pSelected>X</pSelected> this node → X \"I am a selector that gates X (e.g. GainSelector → Gain)\"\n<pVariable Name=\"A\">X</pVariable> this node → X inside a SwissKnife/Converter formula, the symbol A reads X\n<pIsAvailable>X</pIsAvailable> this node → X runtime predicate: I'm only usable when X is truthy\n<pIsImplemented>X</pIsImplemented> this node → X hardware-presence predicate\n<pIsLocked>X</pIsLocked> this node → X \"my value is currently locked\" predicate\n\nReferences are names, not direct pointers. The parser resolves them in pass 2 — see GenApi.parse_nodemap for the three-pass scheme (construct → bind → compile-formulas).","title":"Reference types"},{"category":"section","location":"concepts/#Caching-and-invalidation","page":"Concepts","text":"Each node has a <Cacheable> policy:\n\nNoCache — always re-read from the device.\nWriteThrough — cache reads; cache writes too (assume device accepts the value verbatim).\nWriteAround — cache reads; invalidate on write (re-read next time to discover the device's actual value).\n(default) — cache reads; invalidate on write.\n\n<pInvalidator> declarations build a reverse map at parse time: when node X is written, every node that listed X as an invalidator gets its cache cleared. Same for <pSelected> declarations on selectors — writing the selector invalidates everything it selects.\n\nAfter an explicit set_feature!, GenICam.jl currently wipes the entire nodemap cache (pessimistic but always correct — see features.md for the rationale).","title":"Caching and invalidation"},{"category":"section","location":"concepts/#SFNC:-standard-feature-names","page":"Concepts","text":"The Standard Features Naming Convention is the agreement that every SFNC-compliant camera exposes certain feature names with certain semantics. The mandatory ones for a 2D area-scan camera:\n\nName Type Meaning\nWidth Integer image width in pixels\nHeight Integer image height in pixels\nPixelFormat Enumeration output pixel layout (uses PFNC codes)\nPayloadSize Integer (RO) bytes per frame the camera will deliver\nAcquisitionMode Enumeration SingleFrame / MultiFrame / Continuous\nAcquisitionStart Command begin acquiring\nAcquisitionStop Command stop acquiring\n\nThese seven are sufficient to grab a frame. SFNC also defines a much larger set of recommended features grouped into categories (AcquisitionControl, AnalogControl, ImageFormatControl, DigitalIOControl, DeviceControl, ChunkDataControl, ...). Most of them follow predictable patterns:\n\nSelector chains: TriggerSelector (Enum) chooses which trigger; TriggerMode / TriggerSource / TriggerActivation apply to whichever was selected. Same pattern for GainSelector, ExposureTimeSelector, LineSelector, etc.\nAuto/Manual pairs: ExposureAuto (Off/Once/Continuous) sits next to ExposureTime; setting Auto disables manual control.\n*Raw legacy fallbacks: older cameras exposed ExposureTimeRaw (Integer, ticks) instead of ExposureTime (Float, µs). The set_exposure! helper falls back gracefully.","title":"SFNC: standard feature names"},{"category":"section","location":"concepts/#PFNC:-pixel-format-codes","page":"Concepts","text":"The Pixel Format Naming Convention assigns each format a 32-bit code plus a symbolic name. The code's bit layout encodes structure:\n\nbits 31..24   — Pixel layout marker\n                  0x01 = single-component (mono / Bayer)\n                  0x02 = multi-component  (RGB, BGR, RGBa, ...)\n                  0x03 = multi-part       (3D / depth maps)\n\nbits 23..16   — Effective bits-per-pixel\n                  0x08 = 8-bit, 0x10 = 16-bit, 0x18 = 24-bit, 0x20 = 32-bit, 0x30 = 48-bit, ...\n\nbits 15..0    — Format ID  (vendor-assigned within the layout/bpp class)\n\nExamples decoded:\n\n0x01080001 = Mono8         (single-component, 8 bpp, ID 1)\n0x01080009 = BayerRG8      (single-component, 8 bpp, ID 9)\n0x01100007 = Mono16        (single-component, 16 bpp, ID 7)\n0x02180014 = RGB8          (multi-component, 24 bpp, ID 0x14)\n0x02300033 = RGB16         (multi-component, 48 bpp, ID 0x33)\n0x010C0006 = Mono12Packed  (single-component, 12 bpp, ID 6)\n0x0220001D = RGB10p32      (multi-component, 32 bpp, ID 0x1D)\n\nEach buffer the producer hands us carries its code in BUFFER_INFO_PIXELFORMAT plus a namespace in BUFFER_INFO_PIXELFORMAT_NAMESPACE (legacy GEV vs modern PFNC_32BIT — same numeric code in either, vendor-dependent which they report). GenICam.jl registers each format under both namespaces so the lookup works either way.\n\nThe full format table is in Pixel formats.","title":"PFNC: pixel-format codes"},{"category":"section","location":"concepts/#Putting-it-all-together:-an-acquisition-lifecycle","page":"Concepts","text":"Now we can trace what happens between load_producer and grab(cam):\n\nload_producer(path)\n  └─ Libdl.dlopen(path)\n  └─ Libdl.dlsym(handle, :GCInitLib), ... (resolve every symbol)\n  └─ ccall GCInitLib                       ← producer global init\n                                            (returns ProducerAPI)\n\nProducer(api)\n  └─ ccall TLOpen → TL_HANDLE                ← transport-layer module\n\nlist_cameras(producer)\n  └─ ccall TLUpdateInterfaceList            ← refresh hardware list\n  └─ for each interface:\n       ccall TLOpenInterface → IF_HANDLE\n       ccall IFUpdateDeviceList\n       ccall IFGetDeviceID + IFGetDeviceInfo\n\nopen_camera(producer, ifinfo, devinfo)\n  ├─ ccall TLOpenInterface → IF_HANDLE\n  ├─ ccall IFOpenDevice    → DEV_HANDLE       ← exclusive control of one camera\n  ├─ ccall DevOpenDataStream → DS_HANDLE      ← image-streaming channel\n  ├─ ccall DevGetPort → PORT_HANDLE           ← register-map handle\n  ├─ ccall GCGetPortURL                       ← URL of camera's XML\n  │     \"Local:cam.zip;0xA000;0x12340\"\n  ├─ ccall GCReadPort(port, 0xA000, 0x12340)  ← download the XML bytes\n  ├─ ZipFile decompression\n  ├─ EzXML parse\n  ├─ GenApi parse_nodemap                     ← 3-pass: construct / bind / compile\n  └─ return Camera\n\ncam.Width = 640\n  ├─ getproperty/setproperty! → set_feature!(cam, :Width, 640)\n  ├─ Width is an Integer node → resolve pValue → WidthReg (IntReg)\n  ├─ encode 640 as 4-byte little-endian unsigned\n  └─ ccall GCWritePort(port, WidthReg.address, bytes)\n\ngrab(cam)\n  ├─ Lazy-allocate Acquisition:\n  │    ├─ N × Vector{UInt8}(undef, payload_size)\n  │    ├─ ccall DSAnnounceBuffer × N         ← register each pointer\n  │    ├─ ccall DSQueueBuffer × N            ← into input pool\n  │    └─ ccall GCRegisterEvent(EVENT_NEW_BUFFER) → EVENT_HANDLE\n  ├─ ccall DSStartAcquisition(ds, num_to_acquire = 1)\n  ├─ execute_command!(:AcquisitionStart) → GCWritePort\n  ├─ ccall EventGetData(event, &EVENT_NEW_BUFFER_DATA, timeout)  ← block\n  ├─ Decode pixel format → typed Matrix\n  ├─ ccall DSQueueBuffer (return buffer to pool)\n  ├─ execute_command!(:AcquisitionStop) → GCWritePort\n  ├─ ccall DSStopAcquisition (no-op if producer auto-stopped)\n  └─ return DecodedFrame\n\nThat's it. Every operation in GenICam.jl is some composition of those pieces.","title":"Putting it all together: an acquisition lifecycle"},{"category":"section","location":"concepts/#Further-reading","page":"Concepts","text":"The EMVA standard PDFs (free) at https://www.emva.org:\n\"GenICam Standard\" — top-level GenApi spec\n\"GenICam Standard Features Naming Convention (SFNC)\"\n\"GenICam GenTL Standard\"\n\"GenICam Pixel Format Naming Convention (PFNC)\"\nThe official GenICam header GenTL_v1_5.h — the C ABI this package binds.\nAravis — a pure-C re-implementation that replaces both the consumer and the producer layers; readable reference for many GenApi semantics.\nHarvesters — Python consumer that binds the EMVA reference C++ implementation; useful for cross-checking behaviour.","title":"Further reading"},{"category":"section","location":"features/#Features","page":"Features","text":"A feature is a named piece of camera state — Width, ExposureTime, TriggerMode, DeviceVendorName, etc. The camera publishes every feature it understands in its GenApi XML node-map; GenICam.jl parses that XML on open_camera and exposes each feature for read / write / execute.\n\nIf you're new to the GenApi node-map / register-vs-feature distinction, read Background: GenApi node-map first.","title":"Features"},{"category":"section","location":"features/#Two-API-styles","page":"Features","text":"# Property syntax (the convenience layer)\ncam.Width = 640\nw = cam.Width\ncam.PixelFormat = \"Mono8\"\n\n# Explicit calls (the core, used internally)\nset_feature!(cam, :Width, 640)\nw = get_feature(cam, :Width)\nset_feature!(cam, \"PixelFormat\", \"Mono8\")\n\n# Commands trigger an action — they have no readable value\nexecute_command!(cam, :AcquisitionStart)\nexecute_command!(cam, :TriggerSoftware)\n\nThe two are exact equivalents (the property setter calls set_feature! under the hood). Use whichever reads better in context.","title":"Two API styles"},{"category":"section","location":"features/#Discoverability","page":"Features","text":"propertynames(cam)        # every feature name as a Symbol\nfeatures(cam)              # every feature name as a String\nlength(cam.nodemap.nodes)  # total number of nodes (>1000 typical)\n\nTab-completion works in the REPL: type cam. and press Tab to see all feature names. The list is built once at open_camera time from the camera's <Category> tree (with anything not reachable from Root appended in declaration order).","title":"Discoverability"},{"category":"section","location":"features/#Feature-types","page":"Features","text":"get_feature returns different Julia types depending on the underlying GenApi node:\n\nGenApi node Returns\n<Integer>, <IntReg>, <MaskedIntReg> Int64\n<IntConverter>, <IntSwissKnife> Int64\n<Float>, <FloatReg> Float64\n<Converter>, <SwissKnife> Float64\n<Boolean> Bool\n<String>, <StringReg> String\n<Enumeration> String (the matching EnumEntry name)\n<Command> error — use execute_command! instead\n\nset_feature! accepts the natural Julia type plus, for Enumeration nodes, either the entry name (String or Symbol) or the integer encoding (Int64).","title":"Feature types"},{"category":"section","location":"features/#Categories-and-visibility","page":"Features","text":"The camera vendor groups features into a tree of <Category> nodes for UI display. You can walk that tree:\n\ncategories(cam)\n# Vector{String}: [\"AcquisitionControl\", \"AnalogControl\",\n#                  \"ChunkDataControl\", \"ColorTransformationControl\", ...]\n\nfeatures_in(cam, \"AcquisitionControl\")\n# Vector{String}: [\"AcquisitionMode\", \"ExposureMode\", \"ExposureTime\",\n#                  \"ExposureAuto\", \"AcquisitionFrameRate\", ...]\n\nGenApi marks each feature with a visibility level. :Beginner is \"every camera GUI shows it\"; :Guru is \"vendor experts only\". Filter with features:\n\nfeatures(cam; visibility = :Beginner)             # most-used subset\nfeatures(cam; visibility = :Expert)               # superset of Beginner\nfeatures(cam; visibility = :Guru)                 # everything except Invisible\nfeatures(cam; visibility = :Beginner, category = \"AcquisitionControl\")\n\nThe visibility levels nest: Beginner ⊂ Expert ⊂ Guru ⊂ Invisible.","title":"Categories and visibility"},{"category":"section","location":"features/#Commands","page":"Features","text":"Some features don't have a value — they trigger an action when \"executed\":\n\nexecute_command!(cam, :AcquisitionStart)    # start streaming\nexecute_command!(cam, :AcquisitionStop)     # stop streaming\nexecute_command!(cam, :TriggerSoftware)     # fire one software trigger\nexecute_command!(cam, :DeviceReset)         # power-cycle the camera\n\nCommand nodes raise ArgumentError if you try to read them via get_feature — they're intentionally one-way.","title":"Commands"},{"category":"section","location":"features/#SFNC-mandatory-features","page":"Features","text":"The Standard Features Naming Convention defines a small set of features every compliant 2D area-scan camera must expose. These are the bare minimum needed for image acquisition and they're guaranteed to be present:\n\nName Type Role\nWidth Integer image width in pixels\nHeight Integer image height in pixels\nPixelFormat Enumeration output pixel layout (PFNC code)\nPayloadSize Integer (RO) bytes per frame the camera will deliver\nAcquisitionMode Enumeration SingleFrame / MultiFrame / Continuous\nAcquisitionStart Command begin acquiring\nAcquisitionStop Command stop acquiring\n\ngrab / stream configure those internally; you typically only set Width / Height / PixelFormat explicitly. Beyond the mandatories, SFNC also defines a much larger recommended set of features grouped into categories — AcquisitionControl, AnalogControl, ImageFormatControl, DigitalIOControl, DeviceControl, ChunkDataControl, EventControl, UserSetControl, ...\n\nSee the SFNC overview in Background for the typical patterns (selector chains, Auto/Manual pairs, *Raw legacy fallbacks).","title":"SFNC mandatory features"},{"category":"section","location":"features/#Selector-chains","page":"Features","text":"Multi-channel features (Gain channels, Trigger types, Exposure modes) follow GenApi's selector pattern: write a selector first, then read/write the dependent feature. For example:\n\ncam.GainSelector = \"AnalogAll\"      # which gain channel are we addressing?\ncam.Gain = 6.0                       # set that channel to 6 dB\ncam.GainSelector = \"DigitalAll\"\ncam.Gain = 0.0                       # set the *other* channel to 0 dB\n\n<pSelected> declarations in the camera XML tell GenICam.jl which features depend on which selectors. Writing the selector auto-invalidates dependent caches so the next read goes to the device.","title":"Selector chains"},{"category":"section","location":"features/#Predicates:-pIsAvailable,-pIsImplemented,-pIsLocked","page":"Features","text":"A feature can declare runtime predicates: \"I'm only available when SomeOtherSwitch is on\", \"I'm not implemented on this hardware variant\", etc. GenICam.jl evaluates those predicates before every read / write and raises GenApi.FeatureNotAvailable when one returns false.\n\ntry\n    cam.mvLiquidLensFocus = 50.0\ncatch e\n    e isa GenApi.FeatureNotAvailable && println(\"camera doesn't have a liquid lens\")\nend\n\nThis is what makes the full-feature walk fast: the parser exposes 654 features on a real mvBlueFOX3, but only ~80% are actually accessible on a given hardware variant. The rest are rejected before we try to read absent registers (which would otherwise time out the camera).","title":"Predicates: <pIsAvailable>, <pIsImplemented>, <pIsLocked>"},{"category":"section","location":"features/#Caching","page":"Features","text":"Reads are cached. The cache is invalidated when:\n\nThe same feature is written.\nAny feature listed under the node's <pInvalidator> is written.\nAny selector that reaches the node via <pSelected> is written.\nEVENT_FEATURE_INVALIDATE fires for the feature.\nThe whole nodemap is wiped after a write (current pessimistic policy — correct but coarse; a future refinement may narrow it).\n\nFor per-frame metadata (chunks), the chunk decoder explicitly clears the binding's cache before each read so per-frame values are fresh.","title":"Caching"},{"category":"section","location":"features/#SFNC-convenience-helpers","page":"Features","text":"The seven SFNC mandatories aside, a few feature combos come up so often that thin wrappers are worth their weight:\n\nset_aoi!(cam; x = 0, y = 0, width = 640, height = 480)\nreset_aoi!(cam)                                       # back to sensor max\n\nset_trigger!(cam; selector  = :FrameStart,\n                  mode      = :On,\n                  source    = :Software,\n                  activation = :RisingEdge)\ndisable_trigger!(cam)\n\nset_exposure!(cam, 5000.0)         # µs; falls back to ExposureTimeRaw on legacy GigE\nset_gain!(cam, 6.0)                # dB; falls back to GainRaw on legacy GigE\n\nEach helper applies the writes in the order the SFNC spec mandates (e.g. Width/Height before OffsetX/OffsetY; TriggerSelector → TriggerMode → TriggerSource → TriggerActivation).","title":"SFNC convenience helpers"},{"category":"section","location":"features/#Saving-and-restoring-user-settings","page":"Features","text":"Features tagged <Streamable>true</Streamable> in the camera XML can be snapshotted as XML key-value pairs and restored later:\n\nusing GenICam.GenApi\n\nopen(\"camsetup.xml\", \"w\") do io\n    save_settings(io, cam.nodemap, cam.port, cam.api)\nend\n\n# later, on this camera or another of the same model:\nopen(\"camsetup.xml\") do io\n    load_settings(io, cam.nodemap, cam.port, cam.api)\nend\n\nSelectors are written first so dependent features land on the right channel. Features that fail to read (because of pIsAvailable etc.) are silently skipped; on restore, write failures are also skipped — a single bad feature doesn't lose the whole file.","title":"Saving and restoring user settings"},{"category":"section","location":"features/#Direct-nodemap-access","page":"Features","text":"For introspection or tooling, the parsed nodemap is directly accessible:\n\nnm = cam.nodemap\nnm[\"Width\"]                # IntegerNode\nnm[\"WidthReg\"]             # IntRegNode (the backing register)\nnm[\"WidthReg\"].address     # 0x00010000 (literal contribution)\nnm[\"WidthReg\"].address_spec.terms     # full address composition\n\n# All `IntSwissKnife` nodes:\nfilter(p -> p.second isa GenApi.IntSwissKnifeNode, collect(nm.nodes))\n\nThe structs are documented in API reference.","title":"Direct nodemap access"},{"category":"section","location":"features/#Computed-and-converted-features","page":"Features","text":"Features whose value is derived from a formula (SwissKnife, IntSwissKnife, Converter, IntConverter) are read and written through the same cam.X = value interface; the formula evaluation is transparent. The expression language has its own page covering operator precedence, built-in functions, the FROM / TO convention for converters, and the vendor-quirk handling — see The SwissKnife formula language.","title":"Computed and converted features"},{"category":"section","location":"vendors/#Vendor-notes","page":"Vendor notes","text":"Real-world cameras and producers don't always match the spec letter- for-letter. This page collects the deviations we've actually encountered, the workaround in GenICam.jl, and a pointer to the upstream reference (where applicable).\n\nThe package is verified against:\n\nBalluff Impact Acquire producer (mvGenTLProducer.cti and the PCIe variant) on Windows.\nMATRIX VISION mvBlueFOX3-1013C USB3 Vision camera.\n\nCI runs unit tests on Linux / macOS but those runners don't have producers installed; live-camera assertions self-skip via the isempty(list_producers()) guard in runtests.jl.","title":"Vendor notes"},{"category":"section","location":"vendors/#MATRIX-VISION-mvBlueFOX3-(Balluff-producer)","page":"Vendor notes","text":"","title":"MATRIX VISION mvBlueFOX3 (Balluff producer)"},{"category":"section","location":"vendors/#Chunks:-producer-virtual-port-instead-of-ChunkID","page":"Vendor notes","text":"The mvBlueFOX3 XML declares ~16 Chunk* features (ChunkTimestamp, ChunkLineStatusAll, ChunkExposureTime, ...) but only one <ChunkID> element appears in the entire 936 KB XML. Instead each chunk feature's backing register lives on a producer- managed virtual port (ImageInfoPort), and reading the feature through the GenApi nodemap returns the latest buffer's metadata. The producer routes the underlying register read to the buffer internally.\n\nWorkaround: enable_chunks! detects this case and registers a \"producer-virtual\" binding (chunk_id == 0). decode_chunks! clears the binding's cache and re-reads via get_feature — bypassing DSGetBufferChunkData entirely. Implemented in src/chunks.jl.","title":"Chunks: producer-virtual port instead of <ChunkID>"},{"category":"section","location":"vendors/#ChunkSelector-is-a-literal-only-IntegerNode","page":"Vendor notes","text":"The XML names ChunkSelector an <Enumeration>, but its <pValue> points at an <Integer> (ENB) with <Value>0</Value> and no <pValue>. In other words: the camera supports exactly one selector choice (Image) and the literal Integer enforces it. Trying to set the selector to anything else is a semantic error.\n\nWorkaround: writes that hit a literal-only IntegerNode are treated as a no-op when the value matches, an ArgumentError otherwise. enable_chunks! swallows the selector failure and registers the binding anyway — the chunk is still readable through the producer-virtual path. Implemented in src/GenApi/access.jl (_check_literal_match!).","title":"ChunkSelector is a literal-only IntegerNode"},{"category":"section","location":"vendors/#TriggerSelector-follows-the-same-literal-only-pattern","page":"Vendor notes","text":"TriggerSelector exposes 16 EnumEntry values (FrameStart, AcquisitionStart, ... FrameBurstStart) but its <pValue> is again a literal Integer (XEB) fixed at 0 — meaning only FrameStart is supported. The same workaround applies: set_trigger! silently no-ops the selector write when the value matches.","title":"TriggerSelector follows the same literal-only pattern"},{"category":"section","location":"vendors/#EVENT_FEATURE_INVALIDATE-/-EVENT_FEATURE_CHANGE-not-supported","page":"Vendor notes","text":"Neither event type is implemented by the Balluff producer for this camera — GCRegisterEvent returns GC_ERR_NOT_IMPLEMENTED. on_feature_invalidate / on_feature_change catch that error, log a single @warn, and return a no-op ListenerHandle so user code stays portable.","title":"EVENT_FEATURE_INVALIDATE / EVENT_FEATURE_CHANGE not supported"},{"category":"section","location":"vendors/#Per-frame-chunk-values-can-be-stale","page":"Vendor notes","text":"Because chunks come via the producer-virtual port and the producer only updates them when a new buffer is delivered, repeated reads of the same chunk between two grabs return the same value. This is correct — the chunk reflects the most-recently delivered buffer's metadata — but if your buffer pool starves the camera, the chunk appears not to update. Make sure the streaming consumer keeps up.","title":"Per-frame chunk values can be stale"},{"category":"section","location":"vendors/#FormulaTo-/-FormulaFrom-use-TO-in-both-directions","page":"Vendor notes","text":"The GenApi spec says <FormulaFrom> should reference the input via the variable FROM. Some MATRIX VISION cameras use TO in both formulas regardless of direction. We bind both names to the input value in the converter eval context — formulas work either way without us second-guessing the spec text. See src/GenApi/access.jl (_converter_context).","title":"<FormulaTo> / <FormulaFrom> use TO in both directions"},{"category":"section","location":"vendors/#ChunkPixelFormat-Enumeration-entries-are-incomplete","page":"Vendor notes","text":"The camera reports a chunk pixel-format raw value (e.g. 0x6D4F484E) that doesn't appear in the <EnumEntry> list declared in the XML. Reading cam.ChunkPixelFormat therefore raises a KeyError. This is a single EnumerationNode failure; everything else on the camera works. Read it as ChunkPixelFormatRaw if you need the numeric code.","title":"ChunkPixelFormat Enumeration entries are incomplete"},{"category":"section","location":"vendors/#Producer-interactions-/-quirks","page":"Vendor notes","text":"","title":"Producer interactions / quirks"},{"category":"section","location":"vendors/#Single-EVENT_NEW_BUFFER-per-data-stream","page":"Vendor notes","text":"The producer rejects a second GCRegisterEvent(EVENT_NEW_BUFFER) on a data stream that already has one registered. This means the single-frame grab and continuous stream cannot both have an Acquisition live at the same time — they share the data stream.\n\nWorkaround: start_stream automatically tears down the single-frame buffer pool created by grab before standing up its own. You don't have to think about it.","title":"Single EVENT_NEW_BUFFER per data stream"},{"category":"section","location":"vendors/#Camera-enters-\"error-state\"-after-I/O-timeouts-on-absent-hardware","page":"Vendor notes","text":"Reading registers for hardware that isn't physically attached (mvLiquidLensStatus etc.) provokes ~10 s I/O timeouts in the producer. After several such timeouts the camera enters a state where every subsequent register read fails immediately with GC_ERR_IO. Recovery requires re-opening the camera (or, in extreme cases, physically reconnecting the USB/Ethernet cable).\n\nWorkaround: before any read or write, the access layer evaluates the node's <pIsAvailable> / <pIsImplemented> predicates and raises GenApi.FeatureNotAvailable when one is false — so we never even try to read absent-hardware registers. This is what bumped the full-feature-walk success rate from 58% (with timeouts cascading into a wedged camera) to 99.85%.","title":"Camera enters \"error state\" after I/O timeouts on absent hardware"},{"category":"section","location":"vendors/#Camera-disappears-from-the-bus-after-multiple-kill-cycles","page":"Vendor notes","text":"Repeatedly killing a Julia process mid-acquisition (Ctrl-C, force- killing a hung test) eventually leaves the USB3 device in a state where the producer can't see it anymore. list_cameras returns empty until the camera is physically reconnected or the host's USB host-controller is reset.\n\nWorkaround: none from our side — this is hardware-level. Just let close(cam) run cleanly when possible.","title":"Camera disappears from the bus after multiple kill-cycles"},{"category":"section","location":"vendors/#Reporting-new-quirks","page":"Vendor notes","text":"If you hit a vendor / camera combination that requires special handling, please open an issue with:\n\nThe camera vendor + model + firmware version.\nThe producer name + version (producer_info(p, TL_INFO_VERSION)).\nThe XML of the offending node (extract via GenApi.load_xml(cam.port, cam.api) and grep around the relevant feature name).\nA minimal Julia snippet that reproduces the issue.","title":"Reporting new quirks"},{"category":"section","location":"pixelformats/#Pixel-formats","page":"Pixel formats","text":"Cameras deliver images as raw byte buffers; the pixel format tells GenICam.jl how to decode those bytes into a typed Julia matrix. The producer reports the format via BUFFER_INFO_PIXELFORMAT and the namespace via BUFFER_INFO_PIXELFORMAT_NAMESPACE; together they look up an entry in the package's PFNC registry.","title":"Pixel formats"},{"category":"section","location":"pixelformats/#Return-type-rules","page":"Pixel formats","text":"Each decoder produces a typed Matrix of (height, width) shape, with element type chosen from the JuliaImages standard:\n\nMono 8-bit          → Matrix{Gray{N0f8}}\nMono 10/12/14/16    → Matrix{Gray{N0f16}} (left-shifted to fill the range)\nRGB / BGR 8-bit     → Matrix{RGB{N0f8}} / Matrix{BGR{N0f8}}\nRGB / BGR 10/12/14/16 → Matrix{RGB{N0f16}} / Matrix{BGR{N0f16}}\nRGBa / BGRa 8-bit   → Matrix{RGBA{N0f8}} / Matrix{BGRA{N0f8}}\nBayer (any depth)   → Matrix{Gray{N0fX}} + cfa symbol\nYUV (4:2:2 / 4:1:1 / 4:4:4) → Matrix{RGB{N0f8}} (BT.601 conversion)\n\nWhy N0fN (FixedPointNumbers.jl) and not raw UIntN? Because JuliaImages' pipeline operates on intensities in [0, 1], which means ImageFiltering / ImageTransformations / Images all \"just work\" on the result. To get raw bytes back: reinterpret(UInt8, frame.image).","title":"Return type rules"},{"category":"section","location":"pixelformats/#DecodedFrame","page":"Pixel formats","text":"grab returns a DecodedFrame; the image field is the typed matrix. pixel_format records which PFNC entry was used (e.g. :Mono8, :BayerRG10p, :RGB10p32). For Bayer formats cfa is one of :GRBG, :RGGB, :GBRG, :BGGR; for everything else it's :none.\n\nTo get raw producer bytes without any decoding, use grab_raw.","title":"DecodedFrame"},{"category":"section","location":"pixelformats/#Supported-formats","page":"Pixel formats","text":"Family Code bpp Notes\nMono Mono1p, Mono2p, Mono4p 1 / 2 / 4 sub-byte packed\n Mono8 8 direct\n Mono10, Mono12, Mono14 16 right-justified 10/12/14 in 16-bit container\n Mono10p, Mono12p 10 / 12 tightly packed (modern PFNC)\n Mono10Packed, Mono12Packed 12 legacy GigE Vision packing (2 pix/3 bytes)\n Mono16 16 direct\nBayer 8 BayerGR8, BayerRG8, BayerGB8, BayerBG8 8 CFA tag in frame.cfa\nBayer 10/12 BayerGR10, ... BayerBG12 16 padded\nBayer 10p/12p BayerGR10p, ... BayerBG12p 10 / 12 packed\nBayer 12Packed BayerGR12Packed, ... 12 legacy GigE\nBayer 16 BayerGR16, ... BayerBG16 16 direct\nRGB RGB8, BGR8 24 interleaved\n RGB10, BGR10, RGB12, BGR12, RGB14, RGB16, BGR16 48 padded interleaved\n RGB10p32, BGR10p32 32 three 10-bit channels packed in 32 bits\n RGBa8, BGRa8 32 with alpha\nYUV 4:2:2 YUV422_8_UYVY, YUV422_8 (YUYV) 16 converted to RGB{N0f8}\nYUV 4:1:1 YUV411_8_UYYVYY 12 converted to RGB{N0f8}\nYUV 4:4:4 YUV8_UYV 24 converted to RGB{N0f8}\n\nThe codes are defined by the EMVA's PFNC.h. Each format is registered under multiple namespaces (legacy GEV, PFNC 32-bit, plus a \"namespace 0\" fallback) so the lookup succeeds whether or not the producer populates the namespace field.","title":"Supported formats"},{"category":"section","location":"pixelformats/#Decoding-a-PFNC-code","page":"Pixel formats","text":"Each 32-bit PFNC code packs structural information across three byte ranges:\n\nbits 31..24   — Pixel layout marker\n                  0x01 = single-component (mono / Bayer)\n                  0x02 = multi-component  (RGB, BGR, RGBa, ...)\n                  0x03 = multi-part       (3D / depth maps)\n\nbits 23..16   — Effective bits-per-pixel\n                  0x08 / 0x10 / 0x0A / 0x0C / 0x18 / 0x20 / 0x30 / ...\n\nbits 15..0    — Format ID (vendor-assigned within the layout/bpp class)\n\nExamples:\n\n0x01080001 = Mono8         (single-component, 8 bpp, ID 1)\n0x01080009 = BayerRG8      (single-component, 8 bpp, ID 9)\n0x01100007 = Mono16        (single-component, 16 bpp, ID 7)\n0x02180014 = RGB8          (multi-component, 24 bpp, ID 0x14)\n0x010C0006 = Mono12Packed  (single-component, 12 bpp, ID 6)\n0x0220001D = RGB10p32      (multi-component, 32 bpp, ID 0x1D)\n\nThe pixel-format namespace IDs the producer reports in BUFFER_INFO_PIXELFORMAT_NAMESPACE:\n\nNamespace Meaning\n0 Unknown / unspecified — GenICam.jl falls back to the GenApi PixelFormat enum string\n1 Legacy GEV (GigE Vision)\n2 IIDC (FireWire)\n3 PFNC 16-bit (older 16-bit-code variant)\n4 PFNC 32-bit (current standard, what most modern cameras report)","title":"Decoding a PFNC code"},{"category":"section","location":"pixelformats/#Bayer-policy:-no-demosaicing","page":"Pixel formats","text":"Bayer-pattern images are returned raw — the package does not interpolate the missing color channels. Instead frame.cfa is set to the pattern (:RGGB, :GRBG, etc.) and the user picks a demosaicing package:\n\nusing ImageCore, Images, GenICam\n\nframe = grab(cam)            # PixelFormat = \"BayerRG10\"\nimg = frame.image            # Matrix{Gray{N0f16}}\n@assert frame.cfa === :RGGB\n\n# pick your favourite debayer:\nrgb = demosaic(img, frame.cfa)   # via your own package\n\nThis keeps GenICam.jl focused on standard-compliant decoding; the ecosystem already has solid demosaicing.","title":"Bayer policy: no demosaicing"},{"category":"section","location":"pixelformats/#Type-stability","page":"Pixel formats","text":"grab(cam) returns DecodedFrame{Matrix{T}} where T depends on the camera's current PixelFormat. That makes the return type non-inferable across all formats — Julia sees a Union at the grab call boundary. The standard fix is the function-barrier pattern: pass the result to a worker that specialises on the concrete Matrix{T}:\n\nfunction process(cam)\n    frame = grab(cam)            # union return at this single call\n    _process_typed(frame.image)  # specialised per concrete eltype\nend\n\n# The compiler generates one specialisation per Matrix{T} that ever\n# reaches this function — fully type-stable inside.\nfunction _process_typed(img::AbstractMatrix)\n    histogram = zeros(Int, 256)\n    @inbounds for px in img\n        histogram[Int(reinterpret(UInt8, px)) + 1] += 1\n    end\n    return histogram\nend\n\nIn a tight live-preview loop this is the recommended pattern. For one-shot snapshots the boundary's cost is negligible.","title":"Type stability"},{"category":"section","location":"pixelformats/#Looking-up-format-metadata","page":"Pixel formats","text":"The PFNC registry is exported from GenICam.PixelFormats:\n\nusing GenICam.PixelFormats\n\nspec_for_name(:Mono8)\n# PixelFormatSpec(:Mono8, 8, :mono, :none, :decode_mono8)\n\nspec_for_name(:BayerRG12p)\n# PixelFormatSpec(:BayerRG12p, 12, :bayer, :RGGB, :decode_bayer12p)\n\nspec_for_code(UInt64(0), UInt64(0x01080001))    # by namespace + code\n# PixelFormatSpec(:Mono8, 8, :mono, :none, :decode_mono8)\n\nSee API reference for the full signatures of PixelFormatSpec, spec_for_name, spec_for_code, UnsupportedPixelFormat, and DecodedFrame.","title":"Looking up format metadata"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"Auto-generated from doc-strings on every exported name. Topical overviews live on the dedicated pages — this page is the comprehensive list.","title":"API reference"},{"category":"section","location":"api/#Top-level-package-(GenICam)","page":"API reference","text":"","title":"Top-level package (GenICam)"},{"category":"section","location":"api/#Pixel-formats-(GenICam.PixelFormats)","page":"API reference","text":"","title":"Pixel formats (GenICam.PixelFormats)"},{"category":"section","location":"api/#GenApi-(GenICam.GenApi)","page":"API reference","text":"","title":"GenApi (GenICam.GenApi)"},{"category":"section","location":"api/#GenTL-(GenICam.GenTL)","page":"API reference","text":"","title":"GenTL (GenICam.GenTL)"},{"category":"type","location":"api/#GenICam.Camera","page":"API reference","text":"Camera\n\nThe high-level handle for one open camera. Bundles every layer needed to talk to it:\n\nthe parent Producer / Interface / Device / DataStream\nthe camera's port handle and a parsed Nodemap\nan optional cached single-frame Acquisition (for grab)\nan optional StreamHandle (for stream / start_stream)\nthe chunk-binding cache and last-frame chunk dict\nthe lazy event pump for feature-event listeners\n\nConstruct via open_camera; release via close(cam).\n\n\n\n\n\n","title":"GenICam.Camera"},{"category":"type","location":"api/#GenICam.ChunkBinding","page":"API reference","text":"ChunkBinding\n\nResolved (chunk_id, decoder, name) triple cached on the Camera so per-frame chunk decoding doesn't have to walk the nodemap each time.\n\n\n\n\n\n","title":"GenICam.ChunkBinding"},{"category":"type","location":"api/#GenICam.ChunkBindings","page":"API reference","text":"ChunkBindings\n\nPre-split bindings cache stored on cam.chunk_bindings after enable_chunks!. Holds the two binding flavours separately so decode_chunks! doesn't have to filter the full list per frame — at 30+ fps that filter showed up as two Vector{ChunkBinding} allocations per frame on the streaming hot path.\n\ncanonical — bindings whose nodes carry a <ChunkID> (chunk_id != 0). Decoded by carving bytes out of the buffer payload via DSGetBufferChunkData.\nvirtual   — producer-virtual bindings (chunk_id == 0, e.g. mvBlueFOX). Decoded by reading through the regular nodemap accessor.\n\n\n\n\n\n","title":"GenICam.ChunkBindings"},{"category":"type","location":"api/#GenICam.DeviceEvent","page":"API reference","text":"DeviceEvent\n\nEmitted by watch_devices / watch_all_producers when a device appears or disappears.\n\nkind           — :added or :removed.\nproducer_path  — path to the .cti that reports the change. For                    watch_devices (single producer) it's the path                    of the producer being watched.\ninterface      — info on the parent interface.\ndevice         — info on the device whose state changed.\ntimestamp_ns   — time_ns() at observation.\n\n\n\n\n\n","title":"GenICam.DeviceEvent"},{"category":"type","location":"api/#GenICam.FeatureEvent","page":"API reference","text":"FeatureEvent\n\nThe argument passed to user-supplied callbacks.\n\nkind — :invalidate or :change.\nname — the feature name as reported by the producer.\nvalue — the new value (only for :change; nothing for :invalidate).\ntimestamp_ns — Julia time_ns() at receipt; useful for ordering or logging.\n\n\n\n\n\n","title":"GenICam.FeatureEvent"},{"category":"type","location":"api/#GenICam.ListenerHandle","page":"API reference","text":"ListenerHandle\n\nOpaque token returned by on_feature_invalidate / on_feature_change. Pass it back to remove_listener to unsubscribe.\n\n\n\n\n\n","title":"GenICam.ListenerHandle"},{"category":"type","location":"api/#GenICam.StreamHandle","page":"API reference","text":"StreamHandle\n\nThe handle returned by start_stream. Holds the streaming Channel (in .channel), the background Task, the policy, and the running frames_grabbed / frames_dropped counters.\n\nDon't construct directly — use start_stream or stream.\n\n\n\n\n\n","title":"GenICam.StreamHandle"},{"category":"type","location":"api/#GenICam.StreamPolicy","page":"API reference","text":"StreamPolicy\n\nHow start_stream handles a full delivery channel:\n\nDROP_OLDEST — drop the oldest queued frame to make room for the new one. Best for live preview where freshness matters more than completeness.\nDROP_NEWEST — discard the newly arrived frame instead. Symmetric option.\nBLOCK — block the producer task until the consumer drains. The camera stalls once its buffer pool fills; use only when frames must not be lost.\n\n\n\n\n\n","title":"GenICam.StreamPolicy"},{"category":"method","location":"api/#GenICam.categories-Tuple{Camera}","page":"API reference","text":"categories(cam) -> Vector{String}\n\nList the names of every <Category> node in the parsed nodemap, in declaration order.\n\n\n\n\n\n","title":"GenICam.categories"},{"category":"method","location":"api/#GenICam.chunk_features-Tuple{Camera}","page":"API reference","text":"chunk_features(cam) -> Vector{Symbol}\n\nList the names of every chunk-eligible node — either nodes carrying a GenApi <ChunkID> (canonical mechanism) or \"Chunk*\"-prefixed feature nodes that some vendors (notably MATRIX VISION mvBlueFOX) expose via a producer-side virtual port instead of the <ChunkID> declaration.\n\nExcludes the control nodes ChunkModeActive, ChunkSelector, and ChunkEnable themselves.\n\n\n\n\n\n","title":"GenICam.chunk_features"},{"category":"method","location":"api/#GenICam.close_event_pump!-Tuple{Camera}","page":"API reference","text":"close_event_pump!(cam)\n\nStop the background event-pump task and unregister GenTL events. Called automatically from _finalize_camera; users normally don't invoke it themselves.\n\n\n\n\n\n","title":"GenICam.close_event_pump!"},{"category":"method","location":"api/#GenICam.decode_chunks!-Tuple{GenICam.GenTL.Frame, Camera}","page":"API reference","text":"decode_chunks!(frame, cam) -> frame\n\nIf frame carries chunk data (BUFFER_INFO_CONTAINS_CHUNKDATA == true), ask the producer for the per-chunk offsets/lengths via DSGetBufferChunkData, decode each one against the binding cached on cam, and stash the result in frame.chunks. Returns frame.\n\nIf cam.chunk_bindings is empty, this is a fast no-op.\n\n\n\n\n\n","title":"GenICam.decode_chunks!"},{"category":"method","location":"api/#GenICam.disable_chunks!-Tuple{Camera}","page":"API reference","text":"disable_chunks!(cam)\n\nTurn off chunk mode and clear the binding cache.\n\n\n\n\n\n","title":"GenICam.disable_chunks!"},{"category":"method","location":"api/#GenICam.disable_trigger!-Tuple{Camera}","page":"API reference","text":"disable_trigger!(cam; selector=:FrameStart)\n\nEquivalent to set_trigger!(cam; selector=selector, mode=:Off).\n\n\n\n\n\n","title":"GenICam.disable_trigger!"},{"category":"method","location":"api/#GenICam.enable_chunks!-Tuple{Camera, Vector{Symbol}}","page":"API reference","text":"enable_chunks!(cam, names::Vector{Symbol})\n\nTurn on chunk mode and select each named chunk. The order is important on most cameras: ChunkModeActive must go first, then for each name we set ChunkSelector = name and ChunkEnable = true. Caches the resolved bindings on cam.chunk_bindings so decode_chunks! is fast.\n\nTwo binding flavours are recognised:\n\nCanonical: the chunk-bearing node carries a <ChunkID>. The binding stores that ID and decode_chunks! carves the chunk out of the buffer payload via DSGetBufferChunkData.\nProducer-virtual (mvBlueFOX-style): the chunk feature has no <ChunkID> but its register lives on a producer-managed virtual port (e.g. ImageInfoPort). The binding stores ID 0 and decode_chunks! reads the value through the regular nodemap accessor — the producer routes the read to the buffer's metadata.\n\n\n\n\n\n","title":"GenICam.enable_chunks!"},{"category":"method","location":"api/#GenICam.execute_command!-Tuple{Camera, Union{AbstractString, Symbol}}","page":"API reference","text":"execute_command!(cam, name)\n\n\n\n\n\n","title":"GenICam.execute_command!"},{"category":"method","location":"api/#GenICam.features-Tuple{Camera}","page":"API reference","text":"features(cam; visibility=:Guru, category=nothing) -> Vector{String}\n\nList the camera's user-facing features, optionally filtered.\n\nvisibility is one of :Beginner, :Expert, :Guru, or :Invisible. Features whose declared <Visibility> is more expert than the requested level are hidden. Default :Guru shows everything except :Invisible.\ncategory restricts to features under the named GenApi <Category> branch.\n\n\n\n\n\n","title":"GenICam.features"},{"category":"method","location":"api/#GenICam.features_in-Tuple{Camera, AbstractString}","page":"API reference","text":"features_in(cam, category::AbstractString) -> Vector{String}\n\nList the feature names directly under category, in declaration order. Recursive: if a sub-element is itself a Category, its features are inlined into the result.\n\n\n\n\n\n","title":"GenICam.features_in"},{"category":"method","location":"api/#GenICam.frames_dropped-Tuple{Camera}","page":"API reference","text":"frames_dropped(cam) -> Int\n\nNumber of frames the streaming task has dropped (because the channel was full and the policy was DROP_OLDEST or DROP_NEWEST, or because decode failed for that frame).\n\n\n\n\n\n","title":"GenICam.frames_dropped"},{"category":"method","location":"api/#GenICam.frames_grabbed-Tuple{Camera}","page":"API reference","text":"frames_grabbed(cam) -> Int\n\nNumber of frames the streaming task has successfully delivered to its channel. Counts only frames that were actually pushed; frames dropped by the policy are tracked separately by frames_dropped.\n\n\n\n\n\n","title":"GenICam.frames_grabbed"},{"category":"method","location":"api/#GenICam.get_feature-Tuple{Camera, Union{AbstractString, Symbol}}","page":"API reference","text":"get_feature(cam, name) -> value\n\n\n\n\n\n","title":"GenICam.get_feature"},{"category":"method","location":"api/#GenICam.grab-Tuple{Camera}","page":"API reference","text":"grab(cam; timeout_ms=2000, num_buffers=4) -> DecodedFrame\n\nGrab one frame in single-frame acquisition mode and return it as a DecodedFrame whose image field is a typed Julia matrix matching the camera's current PixelFormat:\n\nMono8 / Mono16 / Mono10 / Mono12 / Mono14 / packed mono variants → Matrix{Gray{N0f8}} or Matrix{Gray{N0f16}}\nRGB8 / BGR8 → Matrix{RGB{N0f8}} / Matrix{BGR{N0f8}}\nRGB10/12/14/16, RGB10p32, BGR10p32 → Matrix{RGB{N0f16}}\nRGBa8 / BGRa8 → Matrix{RGBA{N0f8}} / Matrix{BGRA{N0f8}}\nBayerXX* → Matrix{Gray{N0fX}} plus a cfa tag (no demosaicing — pass the result through Images.jl or your own demosaic to get an RGB image)\nYUV422_*, YUV411_*, YUV8_UYV → Matrix{RGB{N0f8}} (BT.601 conversion)\n\nThe matrix has shape (height, width) with img[row, col] indexing naturally. The acquisition buffer pool is created lazily on the first call and reused on subsequent calls; close(cam) tears it down.\n\nUse grab_raw if you want the unparsed Vector{UInt8} payload.\n\n\n\n\n\n","title":"GenICam.grab"},{"category":"method","location":"api/#GenICam.grab_raw-Tuple{Camera}","page":"API reference","text":"grab_raw(cam; timeout_ms=2000, num_buffers=4) -> Vector{UInt8}\n\nLike grab but skip pixel-format decoding and return the producer's raw payload bytes. The buffer is owned and may be retained — it has been copied out of the GenTL pool before this returns.\n\n\n\n\n\n","title":"GenICam.grab_raw"},{"category":"method","location":"api/#GenICam.img_chunks-Tuple{DecodedFrame}","page":"API reference","text":"img_chunks(decoded_frame) -> Dict{Symbol,Any} or nothing\n\nConvenience accessor: pulls the chunks dict off a DecodedFrame's underlying buffer if one is attached (only available when the camera had chunks enabled at capture time).\n\n\n\n\n\n","title":"GenICam.img_chunks"},{"category":"method","location":"api/#GenICam.is_streaming-Tuple{Camera}","page":"API reference","text":"is_streaming(cam) -> Bool\n\n\n\n\n\n","title":"GenICam.is_streaming"},{"category":"method","location":"api/#GenICam.last_chunks-Tuple{Camera}","page":"API reference","text":"last_chunks(cam) -> Dict{Symbol,Any} or nothing\n\nThe chunks dict from the most recent grab or stream-yielded frame, or nothing if no chunks have been seen yet.\n\n\n\n\n\n","title":"GenICam.last_chunks"},{"category":"method","location":"api/#GenICam.list_all_cameras-Tuple{}","page":"API reference","text":"list_all_cameras(; refresh=true, timeout_ms=1000) ->\n    Vector{NamedTuple{(:producer_path, :interface, :device), ...}}\n\nIterate every .cti producer found via list_producers(), open each in turn, enumerate the cameras it can see, and return one entry per device with its parent producer path tagged.\n\nProducers that fail to load (missing dependencies, vendor lock, etc.) are skipped with @warn; they don't break the walk.\n\n\n\n\n\n","title":"GenICam.list_all_cameras"},{"category":"method","location":"api/#GenICam.list_cameras-Tuple{GenICam.GenTL.Producer}","page":"API reference","text":"list_cameras(producer; refresh=true) -> Vector{NamedTuple}\n\nWalk every interface of producer and return one entry per visible device, each with the parent interface info attached so callers can pass it back to open_camera.\n\n\n\n\n\n","title":"GenICam.list_cameras"},{"category":"method","location":"api/#GenICam.on_feature_change-Tuple{Camera, Function}","page":"API reference","text":"on_feature_change(cam, callback) -> ListenerHandle\non_feature_change(cam, name::AbstractString, callback) -> ListenerHandle\n\nLike on_feature_invalidate but for EVENT_FEATURE_CHANGE — the producer pushes the new value with the event so the callback gets a populated FeatureEvent.value.\n\n\n\n\n\n","title":"GenICam.on_feature_change"},{"category":"method","location":"api/#GenICam.on_feature_invalidate-Tuple{Camera, Function}","page":"API reference","text":"on_feature_invalidate(cam, callback) -> ListenerHandle\non_feature_invalidate(cam, name::AbstractString, callback) -> ListenerHandle\n\nRegister a callback(::FeatureEvent) to fire whenever the camera invalidates a feature (auto-exposure ramp, hardware-driven gain change, etc.). With a name, only events for that feature trigger the callback; without one, all invalidate events do.\n\nReturns a ListenerHandle for later remove_listener. If the producer doesn't support feature events, returns a no-op handle and emits a single @warn (so portable code keeps working).\n\n\n\n\n\n","title":"GenICam.on_feature_invalidate"},{"category":"function","location":"api/#GenICam.open_camera","page":"API reference","text":"open_camera(producer, idx::Integer = 1) -> Camera\n\nConvenience: open the idx-th camera reported by list_cameras(producer).\n\n\n\n\n\n","title":"GenICam.open_camera"},{"category":"method","location":"api/#GenICam.open_camera-Tuple{GenICam.GenTL.Producer, GenICam.GenTL.InterfaceInfo, GenICam.GenTL.DeviceInfo}","page":"API reference","text":"open_camera(producer, interface_info, device_info;\n            access=DEVICE_ACCESS_CONTROL) -> Camera\n\nOpen the full chain (Interface → Device → DataStream) for one device, load its GenApi XML node-map, and return a Camera ready for feature access and acquisition.\n\n\n\n\n\n","title":"GenICam.open_camera"},{"category":"method","location":"api/#GenICam.remove_listener-Tuple{Camera, ListenerHandle}","page":"API reference","text":"remove_listener(cam, handle::ListenerHandle)\n\nUnsubscribe a previously registered listener. No-op if the handle no longer matches a live listener (e.g. the camera has been closed).\n\n\n\n\n\n","title":"GenICam.remove_listener"},{"category":"method","location":"api/#GenICam.reset_aoi!-Tuple{Camera}","page":"API reference","text":"reset_aoi!(cam)\n\nSet the AOI to the full sensor: Width/Height to their max, OffsetX/Y to 0.\n\n\n\n\n\n","title":"GenICam.reset_aoi!"},{"category":"method","location":"api/#GenICam.set_aoi!-Tuple{Camera}","page":"API reference","text":"set_aoi!(cam; x=0, y=0, width, height)\n\nConfigure the active sensor area. Order matters: Width/Height go first (otherwise OffsetX+Width might exceed the current sensor width and the camera rejects the write); we then write OffsetX/OffsetY. We zero the offsets first if the new (width, height) wouldn't fit at the current offset.\n\nwidth/height are clamped against WidthMax/HeightMax (or the GenApi node's max) when those features are exposed.\n\n\n\n\n\n","title":"GenICam.set_aoi!"},{"category":"method","location":"api/#GenICam.set_exposure!-Tuple{Camera, Real}","page":"API reference","text":"set_exposure!(cam, microseconds::Real; auto=:Off)\n\nSet the exposure time in microseconds. Prefers ExposureTime (Float, µs per SFNC); falls back to ExposureTimeRaw (Integer, raw camera ticks) for legacy GigE Vision cameras that don't expose the modern feature.\n\nauto controls ExposureAuto if the feature is present (:Off / :Once / :Continuous).\n\n\n\n\n\n","title":"GenICam.set_exposure!"},{"category":"method","location":"api/#GenICam.set_feature!-Tuple{Camera, Union{AbstractString, Symbol}, Any}","page":"API reference","text":"set_feature!(cam, name, value)\n\n\n\n\n\n","title":"GenICam.set_feature!"},{"category":"method","location":"api/#GenICam.set_gain!-Tuple{Camera, Real}","page":"API reference","text":"set_gain!(cam, db::Real; auto=:Off)\n\nSet the analog gain in decibels. Prefers Gain (Float, dB per SFNC); falls back to GainRaw (Integer, raw register value) for legacy GigE cameras that don't expose the modern feature.\n\n\n\n\n\n","title":"GenICam.set_gain!"},{"category":"method","location":"api/#GenICam.set_trigger!-Tuple{Camera}","page":"API reference","text":"set_trigger!(cam; selector=:FrameStart, mode=:On, source=:Line0,\n                   activation=:RisingEdge)\n\nConfigure the SFNC trigger chain. Writes happen in the spec'd order:\n\nTriggerSelector → TriggerMode → (TriggerSource → TriggerActivation if mode == :On)\n\nsource and activation are skipped when mode == :Off.\n\n\n\n\n\n","title":"GenICam.set_trigger!"},{"category":"method","location":"api/#GenICam.start_stream-Tuple{Camera}","page":"API reference","text":"start_stream(cam; num_buffers=8, decode=true, channel_size=4,\n             policy=DROP_OLDEST, timeout_ms=1000) -> StreamHandle\n\nBegin continuous acquisition on cam. Returns a StreamHandle whose channel field yields decoded frames as they arrive.\n\nOnly one stream may be active per camera. The handle is also stored on cam.stream so that closing the camera tears the stream down cleanly.\n\nArguments:\n\nnum_buffers — size of the producer's buffer pool. Larger values tolerate brief consumer stalls without dropping frames.\ndecode — when true (default), each frame is run through PixelFormats.decode_frame before being pushed; the channel yields DecodedFrame values. When false, the raw Vector{UInt8} payload is copied (so the caller can keep it after requeue!).\nchannel_size — Julia Channel capacity. Combine with policy to decide what happens when the consumer is slower than the producer.\npolicy — DROP_OLDEST, DROP_NEWEST, or BLOCK (see module docs).\ntimeout_ms — per-frame next_frame! timeout. Determines how often the streaming task checks the stop-signal.\n\n\n\n\n\n","title":"GenICam.start_stream"},{"category":"method","location":"api/#GenICam.stop_stream-Tuple{Camera}","page":"API reference","text":"stop_stream(cam) -> nothing\n\nStop the active stream on cam: signal the producer task to exit, send AcquisitionStop to the camera, drain and close the channel, and tear down the acquisition pool. No-op if no stream is active.\n\n\n\n\n\n","title":"GenICam.stop_stream"},{"category":"method","location":"api/#GenICam.stop_watch!-Tuple{Channel{DeviceEvent}}","page":"API reference","text":"stop_watch!(channel::Channel{DeviceEvent})\n\nTell every watcher feeding channel to exit, then close the channel.\n\n\n\n\n\n","title":"GenICam.stop_watch!"},{"category":"method","location":"api/#GenICam.stream-Tuple{Camera}","page":"API reference","text":"stream(cam; kwargs...) -> Channel\n\nConvenience wrapper: returns the streaming channel directly so you can write for img in stream(cam). Forwards all keyword arguments to start_stream. The caller is responsible for stop_stream(cam).\n\n\n\n\n\n","title":"GenICam.stream"},{"category":"method","location":"api/#GenICam.stream-Tuple{Function, Camera}","page":"API reference","text":"stream(f, cam; kwargs...)\n\nDo-block variant — calls f(channel) while streaming, then guarantees stop_stream(cam) is called on exit (success or exception).\n\nimgs = stream(cam) do ch\n    [take!(ch) for _ in 1:30]\nend\n\n\n\n\n\n","title":"GenICam.stream"},{"category":"method","location":"api/#GenICam.watch_all_producers-Tuple{}","page":"API reference","text":"watch_all_producers(; interval=2.0, timeout_ms=500) -> Channel{DeviceEvent}\n\nWatch every producer in GENICAM_GENTL64_PATH simultaneously. Spawns one polling task per .cti; events from all producers funnel into a single channel.\n\n\n\n\n\n","title":"GenICam.watch_all_producers"},{"category":"method","location":"api/#GenICam.watch_devices-Tuple{GenICam.GenTL.Producer}","page":"API reference","text":"watch_devices(producer; interval=2.0, timeout_ms=500) -> Channel{DeviceEvent}\n\nSpawn a polling Task that watches producer. Every interval seconds it refreshes the interface list and (per interface) the device list, diffs against the previous snapshot, and emits a DeviceEvent for each (producer, interface_id, device_id) that appeared or disappeared.\n\nReturns the channel. Call stop_watch!(channel) to terminate.\n\nThe do-block variant watch_devices(f, producer; ...) calls f(channel) inside a try/finally that guarantees stop_watch! runs on exit.\n\n\n\n\n\n","title":"GenICam.watch_devices"},{"category":"type","location":"api/#GenICam.PixelFormats.BufferPool","page":"API reference","text":"BufferPool(capacity::Integer = 4)\n\nRing of reusable decoded-frame matrices. Pass to start_stream (or to decode_frame!) to make the streaming task allocation-free on the hot path.\n\nLazy per-format: only the field matching the camera's pixel format actually gets populated, on the first frame. Subsequent frames of the same shape and type reuse the slots round-robin. If the camera switches format mid-stream (rare), the ring for the new format is allocated on first sight; the old ring becomes garbage on the next collection.\n\n\n\n\n\n","title":"GenICam.PixelFormats.BufferPool"},{"category":"type","location":"api/#GenICam.PixelFormats.DecodedFrame","page":"API reference","text":"DecodedFrame{A<:AbstractArray}\n\nThe result of decode_frame(frame, cam):\n\nimage         — the typed Julia array (Matrix{Gray{N0f8}},                   Matrix{RGB{N0f16}}, ...). Owns its own memory; safe to                   keep after the underlying GenTL buffer is re-queued.\npixel_format  — the PFNC symbol (e.g. :Mono8, :BayerRG8, :RGB10p32).\ncfa           — :none for non-Bayer, otherwise one of :GRBG, :RGGB,                   :GBRG, :BGGR. Use this with a demosaicing package                   (e.g. Images.jl) to render Bayer images.\n\n\n\n\n\n","title":"GenICam.PixelFormats.DecodedFrame"},{"category":"type","location":"api/#GenICam.PixelFormats.PixelFormatSpec","page":"API reference","text":"PixelFormatSpec\n\nDescribes one PFNC pixel format:\n\nname            — symbolic name (:Mono8, :BayerRG12p, :RGB10p32, ...)\nbits_per_pixel  — declared bit depth (used for buffer-size checks)\nfamily          — one of :mono, :bayer, :rgb, :bgr, :rgba,                     :bgra, :yuv422, :yuv411, :yuv444\ncfa             — Bayer pattern (:GRBG/:RGGB/:GBRG/:BGGR) or :none\ndecoder         — Val-dispatch tag for the unpacker\n\n\n\n\n\n","title":"GenICam.PixelFormats.PixelFormatSpec"},{"category":"type","location":"api/#GenICam.PixelFormats.UnsupportedPixelFormat","page":"API reference","text":"UnsupportedPixelFormat <: Exception\n\nRaised by decode_frame when the buffer's pixel format isn't in the PFNC registry (or when an explicit hint doesn't match any known format).\n\n\n\n\n\n","title":"GenICam.PixelFormats.UnsupportedPixelFormat"},{"category":"method","location":"api/#GenICam.PixelFormats.decode_frame!-Tuple{Any, GenICam.GenTL.Frame}","page":"API reference","text":"decode_frame!(pool::BufferPool, frame::Frame; pixel_format_hint=nothing) -> DecodedFrame\n\nSame as decode_frame but routes through a BufferPool so the decoder can write into a pre-allocated matrix instead of allocating a fresh one per frame. Used by start_stream when its buffer_pool keyword is supplied.\n\nFalls back to allocating decode for any format that doesn't have a pool-aware variant (currently the YUV / packed / 16-bit-RGB families) — correctness is preserved, the pool just doesn't help for those.\n\n\n\n\n\n","title":"GenICam.PixelFormats.decode_frame!"},{"category":"method","location":"api/#GenICam.PixelFormats.decode_frame-Tuple{GenICam.GenTL.Frame}","page":"API reference","text":"decode_frame(frame::Frame; pixel_format_hint=nothing) -> DecodedFrame\n\nLook up frame.pixel_format (and frame.pixel_format_namespace) in the PFNC registry, then dispatch to the matching decoder. If the producer didn't populate the namespace field, pass pixel_format_hint = :Mono8 (or similar) to force a specific decoder; this is what Camera.grab does when the GenApi PixelFormat enumeration name is the only ground truth.\n\nThrows UnsupportedPixelFormat when the (namespace, code) pair is not in the registry. To opt out of decoding entirely, use grab_raw from Camera.\n\n\n\n\n\n","title":"GenICam.PixelFormats.decode_frame"},{"category":"method","location":"api/#GenICam.PixelFormats.spec_for_code-Tuple{Integer, Integer}","page":"API reference","text":"spec_for_code(namespace, code) -> PixelFormatSpec or nothing\n\nResolve (BUFFER_INFO_PIXELFORMAT_NAMESPACE, BUFFER_INFO_PIXELFORMAT) to a known format. Returns nothing if the pair is not in the registry.\n\n\n\n\n\n","title":"GenICam.PixelFormats.spec_for_code"},{"category":"method","location":"api/#GenICam.PixelFormats.spec_for_name-Tuple{Symbol}","page":"API reference","text":"spec_for_name(name::Symbol) -> PixelFormatSpec or nothing\n\nResolve a PFNC symbolic name (e.g. :Mono8, :BayerRG12p). Useful as a fallback when the producer doesn't populate the buffer's pixel-format fields and the only ground truth is the GenApi PixelFormat enumeration string.\n\n\n\n\n\n","title":"GenICam.PixelFormats.spec_for_name"},{"category":"constant","location":"api/#GenICam.PixelFormats.PFNC_FORMATS","page":"API reference","text":"Map (namespace::UInt64, code::UInt64) -> PixelFormatSpec.\n\nProducers report the same numeric code under different namespaces depending on vintage. We register each format under every plausible namespace so the lookup succeeds regardless.\n\n\n\n\n\n","title":"GenICam.PixelFormats.PFNC_FORMATS"},{"category":"type","location":"api/#GenICam.GenApi.AddressSpec","page":"API reference","text":"AddressSpec(terms)\n\nThe full address-composition spec for one register: a list of contributions that sum at access time. The most common case is a single :literal term; dynamic register banks add :pnode and/or :pindex siblings.\n\naddress_terms[1] for a bare-<Address> register is exactly the literal, so the legacy register.address::UInt64 field can shadow this for backward compatibility.\n\n\n\n\n\n","title":"GenICam.GenApi.AddressSpec"},{"category":"type","location":"api/#GenICam.GenApi.AddressTerm","page":"API reference","text":"AddressTerm\n\nOne contribution to a register's effective address.\n\nkind === :literal  — literal is the byte address.\nkind === :pnode    — read ref (an IInteger node) and add its value.\nkind === :pindex   — read ref's value, multiply by offset, then add.\n\n\n\n\n\n","title":"GenICam.GenApi.AddressTerm"},{"category":"type","location":"api/#GenICam.GenApi.BooleanNode","page":"API reference","text":"BooleanNode\n\n<Boolean> GenApi feature. The on/off bit lives in pvalue's register; on_value and off_value are the integer encodings (defaults 1/0).\n\n\n\n\n\n","title":"GenICam.GenApi.BooleanNode"},{"category":"type","location":"api/#GenICam.GenApi.CategoryNode","page":"API reference","text":"CategoryNode\n\n<Category> — pure UI organization. features lists child node names in the order the camera vendor wants them displayed.\n\n\n\n\n\n","title":"GenICam.GenApi.CategoryNode"},{"category":"type","location":"api/#GenICam.GenApi.CircularDependency","page":"API reference","text":"CircularDependency <: Exception\n\nRaised when a chain of GenApi node references (pValue / pAddress / pIndex / pVariable / formula recursion) revisits a node already on the resolution stack. Carries the full visit chain so the offending loop is visible.\n\n\n\n\n\n","title":"GenICam.GenApi.CircularDependency"},{"category":"type","location":"api/#GenICam.GenApi.CommandNode","page":"API reference","text":"CommandNode\n\n<Command> GenApi feature: writing command_value to pvalue's register triggers the action. Some cameras use <pCommandValue> (computed value from another node) — that's encoded as command_value_name instead.\n\n\n\n\n\n","title":"GenICam.GenApi.CommandNode"},{"category":"type","location":"api/#GenICam.GenApi.ConverterNode","page":"API reference","text":"ConverterNode / IntConverterNode\n\nBidirectional formula bridge over a backing register-typed pvalue node:\n\nread  : eval_float(formula_from, FROM = read(pvalue))\nwrite : write(pvalue, eval_float(formula_to, TO = value))\n\nConverter operates on Float64; IntConverter on Int64.\n\n\n\n\n\n","title":"GenICam.GenApi.ConverterNode"},{"category":"type","location":"api/#GenICam.GenApi.EnumEntryNode","page":"API reference","text":"EnumEntryNode\n\nA child of an EnumerationNode. Names a possible value of the parent enum and provides the integer encoding.\n\n\n\n\n\n","title":"GenICam.GenApi.EnumEntryNode"},{"category":"type","location":"api/#GenICam.GenApi.EnumerationNode","page":"API reference","text":"EnumerationNode\n\n<Enumeration> GenApi feature: a register-backed integer with named entries. Reading returns the matching EnumEntry name; writing accepts either an EnumEntry name (String/Symbol) or its integer encoding.\n\n\n\n\n\n","title":"GenICam.GenApi.EnumerationNode"},{"category":"type","location":"api/#GenICam.GenApi.EvalContext","page":"API reference","text":"EvalContext\n\nBundles the variable lookup callback used by the evaluator. lookup(name) must return a Real (Int or Float); for nested SwissKnife / IntReg / Converter chains the callback walks the nodemap recursively. The visiting set is the recursion guard — pre-populate it with the root node's name before evaluating.\n\n\n\n\n\n","title":"GenICam.GenApi.EvalContext"},{"category":"type","location":"api/#GenICam.GenApi.FeatureNotAvailable","page":"API reference","text":"FeatureNotAvailable\n\nRaised when a feature's <pIsAvailable> / <pIsImplemented> predicate evaluates to false. Distinct from a genuine read failure: this means the camera declares the feature unavailable (e.g. optional hardware is missing). Callers can try / catch FeatureNotAvailable to skip such features without treating them as errors.\n\n\n\n\n\n","title":"GenICam.GenApi.FeatureNotAvailable"},{"category":"type","location":"api/#GenICam.GenApi.FloatNode","page":"API reference","text":"FloatNode\n\n<Float> GenApi feature. Same structure as IntegerNode but in Float64 (with optional unit / display precision).\n\n\n\n\n\n","title":"GenICam.GenApi.FloatNode"},{"category":"type","location":"api/#GenICam.GenApi.FloatRegNode","page":"API reference","text":"FloatRegNode\n\n<FloatReg> — IEEE 754 register (4 or 8 bytes).\n\n\n\n\n\n","title":"GenICam.GenApi.FloatRegNode"},{"category":"type","location":"api/#GenICam.GenApi.FormulaEvalError","page":"API reference","text":"FormulaEvalError <: Exception\n\nRaised at read/write time for runtime issues in a SwissKnife or Converter formula evaluation: divide-by-zero, using a Float-only function (SQRT/SIN/PI/...) inside an IntSwissKnife, undefined variable, etc. The formula's parse-time validity is enforced by FormulaParseError instead.\n\n\n\n\n\n","title":"GenICam.GenApi.FormulaEvalError"},{"category":"type","location":"api/#GenICam.GenApi.FormulaParseError","page":"API reference","text":"FormulaParseError <: Exception\n\nRaised at XML-load time when a <Formula> / <FormulaTo> / <FormulaFrom> text is unparseable (mismatched parentheses, unexpected character, malformed number literal, ...). Carries the source string and the offending position, so error messages can show where the problem is.\n\n\n\n\n\n","title":"GenICam.GenApi.FormulaParseError"},{"category":"type","location":"api/#GenICam.GenApi.IntRegNode","page":"API reference","text":"IntRegNode\n\n<IntReg> — the bread-and-butter register holding a signed/unsigned integer.\n\naddress::UInt64   — the literal <Address> contribution. If                       <pAddress> / <pIndex> siblings exist, the                       full effective address is computed at access                       time from address_spec.\naddress_spec      — sum-of-terms representation; for a bare-<Address>                       register this contains a single :literal term                       numerically equal to address.\n\n\n\n\n\n","title":"GenICam.GenApi.IntRegNode"},{"category":"type","location":"api/#GenICam.GenApi.IntegerNode","page":"API reference","text":"IntegerNode\n\n<Integer> GenApi feature. Either backed by a register (pvalue names a register-like node) or a constant literal (has_literal is true and value holds it).\n\n\n\n\n\n","title":"GenICam.GenApi.IntegerNode"},{"category":"type","location":"api/#GenICam.GenApi.MaskedIntRegNode","page":"API reference","text":"MaskedIntRegNode\n\n<MaskedIntReg> — IntReg with a bit-field selection (<Bit> or <LSB>/<MSB>).\n\n\n\n\n\n","title":"GenICam.GenApi.MaskedIntRegNode"},{"category":"type","location":"api/#GenICam.GenApi.NodeMetadata","page":"API reference","text":"NodeMetadata\n\nGenApi attributes that every node carries: visibility, cache policy, streamable flag, invalidator/selector reverse maps, predicate-node names (<pIsAvailable> etc.), and the run-time cache slot.\n\nReference fields hold names until parser Pass 2 binds them to actual Node references — at which point the _*_node companion field gets filled. The name field stays so error messages can be produced even when binding partially failed.\n\n\n\n\n\n","title":"GenICam.GenApi.NodeMetadata"},{"category":"type","location":"api/#GenICam.GenApi.Nodemap","page":"API reference","text":"Nodemap\n\nThe parsed GenApi node graph for one camera. nodes is a name→node lookup; feature_names is a flat ordering of feature-grade nodes (everything that shows up in the Category tree under \"Root\", deduplicated).\n\n\n\n\n\n","title":"GenICam.GenApi.Nodemap"},{"category":"type","location":"api/#GenICam.GenApi.RegisterRawNode","page":"API reference","text":"RegisterRawNode\n\n<Register> — raw-bytes register. Read/write returns a Vector{UInt8} unchanged; pixel-format / chunk decoding happens upstream.\n\n\n\n\n\n","title":"GenICam.GenApi.RegisterRawNode"},{"category":"type","location":"api/#GenICam.GenApi.StringNode","page":"API reference","text":"StringNode\n\n<String> GenApi feature. pvalue resolves to a StringReg (or any register-typed node). When has_literal is true the value is fixed.\n\n\n\n\n\n","title":"GenICam.GenApi.StringNode"},{"category":"type","location":"api/#GenICam.GenApi.StringRegNode","page":"API reference","text":"StringRegNode\n\n<StringReg> — fixed-length zero-terminated ASCII / UTF-8 string register.\n\n\n\n\n\n","title":"GenICam.GenApi.StringRegNode"},{"category":"type","location":"api/#GenICam.GenApi.StructEntryNode","page":"API reference","text":"StructEntryNode\n\nA single bit-field entry of a StructRegNode. Read/write delegates to the parent register with the entry's lsb/msb mask applied.\n\n\n\n\n\n","title":"GenICam.GenApi.StructEntryNode"},{"category":"type","location":"api/#GenICam.GenApi.StructRegNode","page":"API reference","text":"StructRegNode\n\n<StructReg> — a register containing multiple <StructEntry> bit-fields.\n\n\n\n\n\n","title":"GenICam.GenApi.StructRegNode"},{"category":"type","location":"api/#GenICam.GenApi.SwissKnifeNode","page":"API reference","text":"SwissKnifeNode / IntSwissKnifeNode\n\nComputed value: evaluates <Formula> (Float64 or Int64 respectively) with <pVariable> bindings. The compiled AST lives in formula_ast; raw text is kept in formula_src for diagnostics.\n\n\n\n\n\n","title":"GenICam.GenApi.SwissKnifeNode"},{"category":"method","location":"api/#GenICam.GenApi._decode_int-Tuple{AbstractVector{UInt8}, GenICam.GenApi.Endianess, GenICam.GenApi.Signedness}","page":"API reference","text":"_decode_int(bytes, endianess, sign) -> Int64\n\nDecode up to 8 bytes into a signed 64-bit integer. Wider registers (which GenApi itself doesn't define for IntReg) raise. Sign-extension is applied when sign === SIGNED and the register is shorter than 64 bits.\n\n\n\n\n\n","title":"GenICam.GenApi._decode_int"},{"category":"method","location":"api/#GenICam.GenApi._encode_int-Tuple{Integer, Integer, GenICam.GenApi.Endianess, GenICam.GenApi.Signedness}","page":"API reference","text":"_encode_int(value, length_bytes, endianess, sign) -> Vector{UInt8}\n\nEncode a signed 64-bit integer into length_bytes of byte data. Truncates silently when value doesn't fit — that's intentional, GenApi register writes are by spec masked to the register width.\n\n\n\n\n\n","title":"GenICam.GenApi._encode_int"},{"category":"method","location":"api/#GenICam.GenApi._extract_register_spec-Tuple{EzXML.Node}","page":"API reference","text":"Extract the register-shaped fields (literal address + AddressSpec, length, sign, endianess, access, port). Some registers are sign-less (Float, String); caller picks which fields it actually uses.\n\n\n\n\n\n","title":"GenICam.GenApi._extract_register_spec"},{"category":"method","location":"api/#GenICam.GenApi._parse_local_url-Tuple{AbstractString}","page":"API reference","text":"_parse_local_url(url) -> (filename, addr, size)\n\nParse the two GenICam local-URL conventions:\n\nLegacy: Local:Mikrotron_MC4080.xml;F0F00000;C16 URI:    local:///path/file.xml?addr=0xF0F00000&size=0xC16\n\nHex strings may or may not carry an 0x prefix.\n\n\n\n\n\n","title":"GenICam.GenApi._parse_local_url"},{"category":"method","location":"api/#GenICam.GenApi._resolve_address-Tuple{GenICam.GenApi.AddressSpec, Function}","page":"API reference","text":"_resolve_address(spec, evaluate_ref) -> UInt64\n\nSum all contributions to produce the effective register address.\n\nevaluate_ref is a callback (name::String) -> Integer that returns the current value of a referenced IInteger node — it stays a callback so this function is testable in isolation (Etappe 1) without needing a full Nodemap + ProducerAPI chain. Etappe 2 supplies the real callback.\n\n\n\n\n\n","title":"GenICam.GenApi._resolve_address"},{"category":"method","location":"api/#GenICam.GenApi._resolve_local_xml_location-Tuple{GenICam.GenTL.ProducerAPI, Ptr{Nothing}, Integer}","page":"API reference","text":"_resolve_local_xml_location(api, port, idx) -> (filename, addr, size)\n\nDetermine where in the register map the XML lives. Prefers the explicit v1.5 query commands (URL_INFO_FILENAME / URL_INFO_FILE_REGISTER_ADDRESS / URL_INFO_FILE_SIZE) and falls back to parsing the URL string for older producers.\n\n\n\n\n\n","title":"GenICam.GenApi._resolve_local_xml_location"},{"category":"method","location":"api/#GenICam.GenApi.eval_float-Tuple{GenICam.GenApi.FormulaAST, GenICam.GenApi.EvalContext}","page":"API reference","text":"eval_float(ast, ctx) -> Float64\n\nEvaluate an AST as SwissKnife / Converter — IEEE 754 double-precision. Supports the full math library (SIN, COS, EXP, LN, SQRT, ...) and constants (PI, E).\n\n\n\n\n\n","title":"GenICam.GenApi.eval_float"},{"category":"method","location":"api/#GenICam.GenApi.eval_int-Tuple{GenICam.GenApi.FormulaAST, GenICam.GenApi.EvalContext}","page":"API reference","text":"eval_int(ast, ctx) -> Int64\n\nEvaluate an AST as IntSwissKnife / IntConverter — pure 64-bit signed integer arithmetic. Each operation truncates intermediate floats to Int64 (per GenApi spec). Functions limited to ABS / SGN / NEG; trig/exp/log raise.\n\n\n\n\n\n","title":"GenICam.GenApi.eval_int"},{"category":"function","location":"api/#GenICam.GenApi.evaluate_int","page":"API reference","text":"evaluate_int(formula, vars::Dict{Symbol,<:Real}) -> Int64\nevaluate_float(formula, vars::Dict{Symbol,<:Real}) -> Float64\n\nParse and evaluate formula against an explicit variable table. Convenience wrappers around parse_formula + eval_int / eval_float.\n\n\n\n\n\n","title":"GenICam.GenApi.evaluate_int"},{"category":"method","location":"api/#GenICam.GenApi.execute!-Tuple{GenICam.GenApi.Nodemap, AbstractString, Ptr{Nothing}, GenICam.GenTL.ProducerAPI}","page":"API reference","text":"execute!(nm, name, port, api)\n\nTrigger a Command node by writing its <CommandValue> to the backing register.\n\n\n\n\n\n","title":"GenICam.GenApi.execute!"},{"category":"method","location":"api/#GenICam.GenApi.get_value-Tuple{GenICam.GenApi.Nodemap, AbstractString, Ptr{Nothing}, GenICam.GenTL.ProducerAPI}","page":"API reference","text":"get_value(nm, name, port, api) -> value\n\nRead a feature node and return its current value. Type depends on the node's category: Int64 for Integer/IntConverter/IntSwissKnife, Float64 for Float/Converter/SwissKnife, Bool for Boolean, String for String / EnumEntry name.\n\n\n\n\n\n","title":"GenICam.GenApi.get_value"},{"category":"method","location":"api/#GenICam.GenApi.is_feature-Tuple{GenICam.GenApi.Node}","page":"API reference","text":"is_feature(n) -> Bool\n\nTrue iff n is one of the user-facing \"feature\" node types — the things that should appear in propertynames(camera). Excludes register and structural nodes.\n\n\n\n\n\n","title":"GenICam.GenApi.is_feature"},{"category":"method","location":"api/#GenICam.GenApi.is_register-Tuple{GenICam.GenApi.Node}","page":"API reference","text":"is_register(n) -> Bool\n\nTrue iff n is a RegisterNode subtype (i.e. has an address + length).\n\n\n\n\n\n","title":"GenICam.GenApi.is_register"},{"category":"method","location":"api/#GenICam.GenApi.load_settings-Tuple{IO, GenICam.GenApi.Nodemap, Ptr{Nothing}, GenICam.GenTL.ProducerAPI}","page":"API reference","text":"load_settings(io, nm, port, api)\n\nInverse of save_settings: parse an XML snapshot from io and write each listed feature back to the camera. Features not present in nm are skipped silently.\n\n\n\n\n\n","title":"GenICam.GenApi.load_settings"},{"category":"method","location":"api/#GenICam.GenApi.load_xml-Tuple{Ptr{Nothing}, GenICam.GenTL.ProducerAPI}","page":"API reference","text":"load_xml(port_handle, api) -> String\n\nRead the producer's first port URL, fetch the XML bytes, decompress if needed, and return the XML document as a String.\n\n\n\n\n\n","title":"GenICam.GenApi.load_xml"},{"category":"method","location":"api/#GenICam.GenApi.metadata-Tuple{GenICam.GenApi.Node}","page":"API reference","text":"metadata(n::Node) -> NodeMetadata\n\nCommon-metadata accessor. Every concrete node type stores it in meta.\n\n\n\n\n\n","title":"GenICam.GenApi.metadata"},{"category":"method","location":"api/#GenICam.GenApi.name-Tuple{GenICam.GenApi.Node}","page":"API reference","text":"name(n::Node) -> String\n\nEvery node knows its own name; this accessor lets generic code retrieve it without dispatching on type.\n\n\n\n\n\n","title":"GenICam.GenApi.name"},{"category":"method","location":"api/#GenICam.GenApi.parse_formula-Tuple{AbstractString}","page":"API reference","text":"parse_formula(src) -> FormulaAST\n\nParse a SwissKnife / Converter formula string into an AST. Throws FormulaParseError on any lex / parse problem.\n\n\n\n\n\n","title":"GenICam.GenApi.parse_formula"},{"category":"method","location":"api/#GenICam.GenApi.parse_nodemap-Tuple{AbstractString}","page":"API reference","text":"parse_nodemap(xml::AbstractString) -> Nodemap\n\nBuild a complete Nodemap from the camera's XML description.\n\n\n\n\n\n","title":"GenICam.GenApi.parse_nodemap"},{"category":"method","location":"api/#GenICam.GenApi.save_settings-Tuple{IO, GenICam.GenApi.Nodemap, Ptr{Nothing}, GenICam.GenTL.ProducerAPI}","page":"API reference","text":"save_settings(io, nm, port, api)\n\nWalk every streamable feature in nm (in feature_names order — which already respects the Category tree, putting selectors near their dependents) and write its current value to io as an XML document.\n\n\n\n\n\n","title":"GenICam.GenApi.save_settings"},{"category":"method","location":"api/#GenICam.GenApi.set_value!-Tuple{GenICam.GenApi.Nodemap, AbstractString, Any, Ptr{Nothing}, GenICam.GenTL.ProducerAPI}","page":"API reference","text":"set_value!(nm, name, value, port, api)\n\nWrite a value to a feature node. Caching is invalidated for this node and any node that lists it via <pInvalidator> or <pSelected>.\n\n\n\n\n\n","title":"GenICam.GenApi.set_value!"},{"category":"method","location":"api/#GenICam.GenApi.tokenize-Tuple{AbstractString}","page":"API reference","text":"tokenize(src) -> Vector{Token}\n\nEagerly turn a formula string into a token stream. Whitespace is skipped. Returns ending with a single TOK_EOF token to simplify the parser's look-ahead.\n\n\n\n\n\n","title":"GenICam.GenApi.tokenize"},{"category":"type","location":"api/#GenICam.GenTL.Acquisition","page":"API reference","text":"Acquisition(ds; num_buffers=4, payload_size=nothing)\n\nSet up an acquisition on ds (a DataStream):\n\nDetermines the buffer size — uses payload_size if given, else queries STREAM_INFO_PAYLOAD_SIZE.\nAllocates num_buffers Julia vectors of that size.\nAnnounces and queues each one.\nRegisters an EVENT_NEW_BUFFER event so subsequent calls to next_frame! can block on it.\n\nThe Acquisition is not yet running — call start!(acq) to begin.\n\n\n\n\n\n","title":"GenICam.GenTL.Acquisition"},{"category":"type","location":"api/#GenICam.GenTL.AcquisitionBuffer","page":"API reference","text":"AcquisitionBuffer\n\nOne announced buffer. Owns:\n\ndata : the Julia-allocated Vector{UInt8} whose pointer we gave the producer\nhandle : the opaque BUFFER_HANDLE returned by DSAnnounceBuffer\n\n\n\n\n\n","title":"GenICam.GenTL.AcquisitionBuffer"},{"category":"type","location":"api/#GenICam.GenTL.Frame","page":"API reference","text":"Frame\n\nLightweight view onto a single completed acquisition buffer. The data field aliases the producer's pixel memory (which is the same Vector{UInt8} we announced) — copy it before the buffer is re-queued if you need to keep the bytes around.\n\nchunks holds the per-frame metadata dict once decode_chunks! has been run; otherwise it's nothing. Mutable so the chunks layer can attach without rebuilding the whole struct.\n\n\n\n\n\n","title":"GenICam.GenTL.Frame"},{"category":"type","location":"api/#GenICam.GenTL.GenTLError","page":"API reference","text":"GenTLError <: Exception\n\nWraps a non-zero return from any GenTL function. Carries the numeric code, the spec'd name (e.g. \"GC_ERR_TIMEOUT\"), and the producer's last-error message (from GCGetLastError). Every ccall that returns a GC_ERROR is checked and a GenTLError is thrown on non-zero.\n\n\n\n\n\n","title":"GenICam.GenTL.GenTLError"},{"category":"method","location":"api/#GenICam.GenTL.ds_get_buffer_chunk_data-Tuple{GenICam.GenTL.ProducerAPI, Ptr{Nothing}, Ptr{Nothing}}","page":"API reference","text":"ds_get_buffer_chunk_data(api, ds, buffer) -> Vector{SINGLE_CHUNK_DATA}\n\nTwo-pass query of DSGetBufferChunkData: first call gets the chunk count, second fills the array. Each SINGLE_CHUNK_DATA carries (ChunkID, ChunkOffset, ChunkLength) so the consumer can carve the payload bytes out of the buffer's BUFFER_INFO_BASE region.\n\n\n\n\n\n","title":"GenICam.GenTL.ds_get_buffer_chunk_data"},{"category":"method","location":"api/#GenICam.GenTL.errorname-Tuple{Integer}","page":"API reference","text":"errorname(code) -> String\n\nLook up the symbolic name (\"GC_ERR_TIMEOUT\" etc.) of a GC_ERROR return code. Returns \"GC_ERR_UNKNOWN(<code>)\" for codes outside the spec'd range.\n\n\n\n\n\n","title":"GenICam.GenTL.errorname"},{"category":"method","location":"api/#GenICam.GenTL.event_get_data-Union{Tuple{T}, Tuple{GenICam.GenTL.ProducerAPI, Ptr{Nothing}, Type{T}, Integer}} where T","page":"API reference","text":"event_get_data(api, event, ::Type{T}, timeout_ms) -> T\n\nBlock up to timeout_ms for the next event, then return the producer's event payload reinterpreted as T (typically EVENT_NEW_BUFFER_DATA). Use GENTL_INFINITE for timeout_ms to wait forever.\n\n\n\n\n\n","title":"GenICam.GenTL.event_get_data"},{"category":"method","location":"api/#GenICam.GenTL.event_get_data_bytes-Tuple{GenICam.GenTL.ProducerAPI, Ptr{Nothing}, Integer}","page":"API reference","text":"event_get_data_bytes(api, event, timeout_ms) -> Vector{UInt8}\n\nReceive an event whose payload is a variable-length byte buffer (typically a EVENT_FEATURE_INVALIDATE payload — a NUL-terminated feature name). Two-pass: first call queries the size, then we allocate and read.\n\n\n\n\n\n","title":"GenICam.GenTL.event_get_data_bytes"},{"category":"method","location":"api/#GenICam.GenTL.gc_read_port-Tuple{GenICam.GenTL.ProducerAPI, Ptr{Nothing}, Integer, Integer}","page":"API reference","text":"gc_read_port(api, port, address, nbytes) -> Vector{UInt8}\n\nRead nbytes from the port's register space starting at address.\n\n\n\n\n\n","title":"GenICam.GenTL.gc_read_port"},{"category":"method","location":"api/#GenICam.GenTL.last_error-Tuple{GenICam.GenTL.ProducerAPI}","page":"API reference","text":"last_error(api) -> (code, message)\n\nPull the producer's most recent error via GCGetLastError. Used to enrich GenTLError exceptions; safe to call even on success (returns code 0 and \"\").\n\n\n\n\n\n","title":"GenICam.GenTL.last_error"},{"category":"method","location":"api/#GenICam.GenTL.list_devices-Tuple{GenICam.GenTL.Interface}","page":"API reference","text":"list_devices(interface; refresh=true, timeout_ms=1000) -> Vector{DeviceInfo}\n\nEnumerate all devices visible on an interface. Caller may iterate the result and pass any DeviceInfo to open_device to open it.\n\n\n\n\n\n","title":"GenICam.GenTL.list_devices"},{"category":"method","location":"api/#GenICam.GenTL.list_interfaces-Tuple{GenICam.GenTL.Producer}","page":"API reference","text":"list_interfaces(producer; refresh=true, timeout_ms=1000) -> Vector{InterfaceInfo}\n\nEnumerate all interfaces visible to a producer. By default the producer's internal interface list is refreshed first via TLUpdateInterfaceList, which is needed on most producers before they will report any GigE/USB interfaces.\n\n\n\n\n\n","title":"GenICam.GenTL.list_interfaces"},{"category":"method","location":"api/#GenICam.GenTL.list_producers-Tuple{}","page":"API reference","text":"list_producers() -> Vector{String}\n\nScan every directory listed in GENICAM_GENTL64_PATH for *.cti files and return their absolute paths. Missing or empty directories are skipped silently. On macOS, additionally probes /Library/Frameworks and ~/Library/Frameworks as fallback locations.\n\n\n\n\n\n","title":"GenICam.GenTL.list_producers"},{"category":"method","location":"api/#GenICam.GenTL.load_producer-Tuple{AbstractString}","page":"API reference","text":"load_producer(path) -> ProducerAPI\n\nOpen a GenTL producer DLL, resolve all required exports, and call GCInitLib. The returned ProducerAPI owns the dlopen handle and must be closed via unload_producer (or it will be closed automatically by the finalizer).\n\n\n\n\n\n","title":"GenICam.GenTL.load_producer"},{"category":"method","location":"api/#GenICam.GenTL.next_frame!-Tuple{GenICam.GenTL.Acquisition}","page":"API reference","text":"next_frame!(acq; timeout_ms=1000) -> Frame\n\nBlock up to timeout_ms for the producer to signal a new buffer, then return a Frame whose data aliases the buffer's bytes. The buffer is not yet re-queued — call requeue!(acq, frame) after consuming the data.\n\n\n\n\n\n","title":"GenICam.GenTL.next_frame!"},{"category":"method","location":"api/#GenICam.GenTL.next_frame_or_timeout-Tuple{GenICam.GenTL.Acquisition}","page":"API reference","text":"next_frame_or_timeout(acq; timeout_ms=1000) -> Frame or nothing\n\nLike next_frame!, but returns nothing on GC_ERR_TIMEOUT instead of throwing. Useful for streaming loops that need to poll a stop-signal between frames without paying the exception-throwing cost.\n\n\n\n\n\n","title":"GenICam.GenTL.next_frame_or_timeout"},{"category":"method","location":"api/#GenICam.GenTL.open_device-Tuple{GenICam.GenTL.Interface, AbstractString}","page":"API reference","text":"open_device(interface, id_or_info; access=DEVICE_ACCESS_CONTROL) -> Device\n\n\n\n\n\n","title":"GenICam.GenTL.open_device"},{"category":"method","location":"api/#GenICam.GenTL.open_interface-Tuple{GenICam.GenTL.Producer, AbstractString}","page":"API reference","text":"open_interface(producer, id_or_info) -> Interface\n\n\n\n\n\n","title":"GenICam.GenTL.open_interface"},{"category":"method","location":"api/#GenICam.GenTL.requeue!-Tuple{GenICam.GenTL.Acquisition, GenICam.GenTL.Frame}","page":"API reference","text":"requeue!(acq, frame)\n\nReturn a Frame's underlying buffer to the input pool so it can be filled again. Must be called for every frame obtained via next_frame! while the acquisition is still running.\n\n\n\n\n\n","title":"GenICam.GenTL.requeue!"},{"category":"method","location":"api/#GenICam.GenTL.start!-Tuple{GenICam.GenTL.Acquisition}","page":"API reference","text":"start!(acq; num_to_acquire=GENTL_INFINITE)\n\nStart the data stream. Subsequent next_frame! calls will block until the producer signals a new buffer.\n\n\n\n\n\n","title":"GenICam.GenTL.start!"},{"category":"method","location":"api/#GenICam.GenTL.stop!-Tuple{GenICam.GenTL.Acquisition}","page":"API reference","text":"stop!(acq; kill=false)\n\nStop the data stream. If kill=true the acquisition engine is told to abort in-flight buffers (ACQ_STOP_FLAGS_KILL); otherwise it finishes the current buffer cleanly (ACQ_STOP_FLAGS_DEFAULT).\n\n\n\n\n\n","title":"GenICam.GenTL.stop!"},{"category":"method","location":"api/#GenICam.GenTL.unload_producer-Tuple{GenICam.GenTL.ProducerAPI}","page":"API reference","text":"unload_producer(api)\n\nTear down a producer: GCCloseLib if it was initialized, then dlclose the library. Safe to call multiple times — subsequent calls are no-ops.\n\n\n\n\n\n","title":"GenICam.GenTL.unload_producer"},{"category":"section","location":"swissknife/#The-SwissKnife-formula-language","page":"SwissKnife","text":"Camera vendors don't always expose features as simple registers. Sometimes a \"feature\" is computed from several others: MaxExposure might be 1_000_000 / FrameRate; EffectiveBandwidth might be a piecewise function of PixelFormat, Width, and Height; the hardware register address itself might be `BaseAddress + ChannelIndex\n\n4`.\n\nGenApi expresses these computations in two node types:\n\n<SwissKnife> (Float) and <IntSwissKnife> (Int) — pure computed values: read-only, take some pVariable inputs, evaluate a <Formula>, return a number.\n<Converter> (Float) and <IntConverter> (Int) — bidirectional bridge over a backing register: a <FormulaFrom> for read (register-units → user-units) and a <FormulaTo> for write (user-units → register-units).\n\nBoth node families share the same expression language — a small but non-trivial mini-language defined inside the GenApi standard. GenICam.jl parses every formula at XML-load time into a cached AST, then walks the AST on each read or write. The full operator set, all spec'd functions, and both Int and Float evaluators are implemented; this page documents what's supported.","title":"The SwissKnife formula language"},{"category":"section","location":"swissknife/#Number-literals","page":"SwissKnife","text":"Form Example Type\nDecimal integer 42, -7 Int64\nHexadecimal 0xFF, 0x1FFFE Int64 (decoded as UInt64 then reinterpreted)\nDecimal float 1.5, -3.14 Float64\nScientific notation 1.5e3, 2.0E-7 Float64\n\nIn an IntSwissKnife / IntConverter, float literals are truncated to Int64 on use (per spec).","title":"Number literals"},{"category":"section","location":"swissknife/#Operators","page":"SwissKnife","text":"Highest precedence first; same-row entries are equal.\n\nLevel Operators Associativity Notes\n1 (high) (...), function call f(args) n/a grouping\n2 unary -, ~, ! right numeric negate / bitwise NOT / logical NOT\n3 ** right exponent (right-associative: 2**3**2 == 2**9 == 512)\n4 *, /, % left int / is div (truncate toward zero); float / is regular\n5 +, - left binary\n6 <<, >> left bit shifts\n7 & left bitwise AND\n8 ^ left bitwise XOR (caret means XOR here, not power!)\n9 | left bitwise OR\n10 <, >, <=, >=, ==/=, !=/<> left comparison; = and <> are GenApi spellings of == and !=\n11 && left short-circuit logical AND\n12 || left short-circuit logical OR\n13 (low) cond ? then : else right ternary; nests right-associatively\n\nTwo surprises worth keeping in mind:\n\n^ is bitwise XOR, not exponentiation. Use ** for power.\n&& / \\|\\| short-circuit; & / \\| always evaluate both sides. For predicates this matters when the right-hand side has side effects through pVariable (i.e. triggers a register read).","title":"Operators"},{"category":"section","location":"swissknife/#Built-in-functions","page":"SwissKnife","text":"","title":"Built-in functions"},{"category":"section","location":"swissknife/#Available-everywhere","page":"SwissKnife","text":"ABS(x) — absolute value\nSGN(x) — sign: -1 / 0 / 1\nNEG(x) — unary negate (same as -x)","title":"Available everywhere"},{"category":"section","location":"swissknife/#Available-only-in-SwissKnife-/-Converter-(Float64-evaluator)","page":"SwissKnife","text":"Function Description\nSIN(x), COS(x), TAN(x) trig (radians)\nASIN(x), ACOS(x), ATAN(x) inverse trig\nATAN2(y, x) two-arg arctangent\nEXP(x) natural exponent\nLN(x) natural log\nLG(x) common log (base 10)\nSQRT(x) square root\nCEIL(x), FLOOR(x), TRUNC(x) rounding\nROUND(x) round to nearest\nROUND(x, n) round to n decimal places\n\nIntSwissKnife / IntConverter reject these (raises GenApi.FormulaEvalError) — the spec forbids float math in the integer evaluator.","title":"Available only in SwissKnife / Converter (Float64 evaluator)"},{"category":"section","location":"swissknife/#Constants","page":"SwissKnife","text":"PI — 3.141592653589793...\nE  — 2.718281828459045...\n\nFloat-only. IntSwissKnife rejects both.","title":"Constants"},{"category":"section","location":"swissknife/#Variables","page":"SwissKnife","text":"Variables are introduced by <pVariable> child elements and bound to other nodes:\n\n<IntSwissKnife Name=\"MaxBufferCount\">\n    <pVariable Name=\"ImgSize\">PayloadSize</pVariable>\n    <Formula>(64 * 1024 * 1024) / ImgSize</Formula>\n</IntSwissKnife>\n\nWhen the formula is evaluated, every reference to ImgSize triggers a fresh read of the PayloadSize node — recursively, with cycle detection (a self-referencing chain raises GenApi.CircularDependency).\n\nIn <Converter> / <IntConverter>, two implicit variables are bound in addition:\n\nFROM — bound on read-path. The current register value, before FormulaFrom runs.\nTO — bound on write-path. The user-supplied value, before FormulaTo runs.\n\nThe spec is clear which goes where, but in practice many vendors (notably MATRIX VISION) use TO in both formulas regardless of direction. GenICam.jl handles this pragmatically: in either path, both FROM and TO resolve to the same input value. See Vendor notes for the rationale.","title":"Variables"},{"category":"section","location":"swissknife/#Examples-from-real-cameras","page":"SwissKnife","text":"These are taken verbatim from cameras GenICam.jl has been tested against (vendor names redacted):\n\n<!-- IntSwissKnife: clamp Height to even multiples up to the sensor max -->\n<IntSwissKnife Name=\"HeightClamp\">\n    <pVariable Name=\"MAX\">SensorHeightMax</pVariable>\n    <pVariable Name=\"REQ\">RequestedHeight</pVariable>\n    <Formula>(REQ &gt; MAX ? MAX : REQ) &amp; ~1</Formula>\n</IntSwissKnife>\n\n<!-- Converter: ExposureTime (microseconds) <-> ExposureTimeRaw (ticks of a 100ns clock) -->\n<Converter Name=\"ExposureTime\">\n    <pValue>ExposureTimeRaw</pValue>\n    <FormulaTo>TO * 10</FormulaTo>\n    <FormulaFrom>FROM / 10</FormulaFrom>\n</Converter>\n\n<!-- IntConverter with a unit and a representation: Bandwidth in Bps -->\n<IntConverter Name=\"DeviceLinkThroughputBps\">\n    <pValue>DeviceLinkThroughputRaw</pValue>\n    <pVariable Name=\"MUL\">MagicConstant</pVariable>\n    <FormulaTo>(TO + MUL - 1) / MUL</FormulaTo>\n    <FormulaFrom>FROM * MUL</FormulaFrom>\n</IntConverter>\n\n<!-- Trigger-source enum quirk: vendor-specific routing -->\n<IntSwissKnife Name=\"TriggerActivationCode\">\n    <pVariable Name=\"MODE\">TriggerMode</pVariable>\n    <pVariable Name=\"OL\">OvertriggerLatchEnable</pVariable>\n    <Formula>(MODE = 1) ? 1 : (OL ? 14 : 0)</Formula>\n</IntSwissKnife>\n\n<!-- SwissKnife: max bandwidth before frames start dropping -->\n<SwissKnife Name=\"MaxFrameRate\">\n    <pVariable Name=\"W\">Width</pVariable>\n    <pVariable Name=\"H\">Height</pVariable>\n    <pVariable Name=\"BPP\">BytesPerPixel</pVariable>\n    <pVariable Name=\"LINK\">LinkSpeed</pVariable>\n    <Formula>LINK / (W * H * BPP * 1.05)</Formula>\n</SwissKnife>\n\nNote the XML-entity escaping: &gt; for >, &lt; for <, &amp; for &. The parser un-escapes these before tokenising.","title":"Examples from real cameras"},{"category":"section","location":"swissknife/#Evaluation-model","page":"SwissKnife","text":"The Julia evaluator uses a Pratt parser → AST → tree-walking interpreter. Two entry points share one AST type — the difference is the eval function:\n\nGenApi.eval_int for IntSwissKnife / IntConverter. All operations stay in Int64, / is div (truncate toward zero), trig/exp/log raise.\nGenApi.eval_float for SwissKnife / Converter. Standard IEEE 754 double-precision.\n\nCompilation happens once, at parse time. Each <Formula> / <FormulaTo> / <FormulaFrom> becomes a small immutable AST stored on the formula node. Repeated reads pay only the tree-walk cost; the tokeniser and parser run zero times after parse_nodemap returns.\n\nYou can use the evaluator outside the camera context for tests or exploration:\n\nusing GenICam.GenApi\n\nevaluate_int(\"(2 + 3) * 4\")                                       # 20\nevaluate_int(\"(W * H + 7) / 8\", Dict(:W => 640, :H => 480))       # 38400\nevaluate_float(\"SQRT(2 * PI)\")                                    # 2.5066...\nevaluate_float(\"(FROM = 1) ? 1 : (OL ? 14 : 0)\",\n               Dict(:FROM => 1, :OL => 0))                        # 1.0","title":"Evaluation model"},{"category":"section","location":"swissknife/#Errors","page":"SwissKnife","text":"GenApi.FormulaParseError — raised at XML-load time when a <Formula> is unparseable (mismatched parens, unexpected character, ...). Carries the source string and a position pointer.\nGenApi.FormulaEvalError — raised at read/write time for runtime issues: divide by zero, using a Float-only function in IntSwissKnife, undefined variable, etc.\nGenApi.CircularDependency — raised when a chain of pVariable resolutions revisits a node already on the stack.","title":"Errors"},{"category":"section","location":"chunks_events/#Chunks-and-events","page":"Chunks and events","text":"Two related-but-distinct mechanisms for out-of-band camera state:\n\nChunks — per-frame metadata appended to (or interleaved with) each image. Things like the exposure time at capture, an incrementing FrameID, a hardware timestamp, the trigger source, the GPIO line state. Captured atomically with the frame.\nEvents — asynchronous notifications fired by the producer when the camera changes a feature on its own (auto-exposure ramping ExposureTime, etc.). Rare in practice but the GenTL standard defines them.","title":"Chunks and events"},{"category":"section","location":"chunks_events/#Chunks","page":"Chunks and events","text":"","title":"Chunks"},{"category":"section","location":"chunks_events/#Discovery","page":"Chunks and events","text":"chunk_features(cam)\n# Vector{Symbol} of every chunk-eligible node in the parsed nodemap.\n# Includes both `<ChunkID>`-tagged nodes (canonical) and `Chunk*`-prefixed\n# nodes that some vendors expose via a producer-virtual port.\n\nThe control nodes themselves (ChunkModeActive, ChunkSelector, ChunkEnable) are intentionally excluded — those configure chunks, they're not chunk values.","title":"Discovery"},{"category":"section","location":"chunks_events/#Enabling","page":"Chunks and events","text":"enable_chunks!(cam, [:ChunkTimestamp, :ChunkExposureTime, :ChunkLineStatusAll])\n\nInternally this:\n\nSets ChunkModeActive = true to put the camera in chunk mode.\nFor each name, writes ChunkSelector = \"<bare name>\" then ChunkEnable = true to mark that chunk for inclusion.\nResolves and caches a ChunkBinding per name so future decode_chunks! calls don't have to walk the nodemap.\n\nSelector / Enable writes are best-effort: some cameras (mvBlueFOX e.g.) only support a single Image selector and bundle every chunk together. If a selector write fails, the binding is still registered and the chunk is read directly through the nodemap when frames arrive.","title":"Enabling"},{"category":"section","location":"chunks_events/#Reading","page":"Chunks and events","text":"After enable_chunks!, every grab automatically populates its Frame's chunks field. The latest dict is also stashed on the camera as last_chunks:\n\nimg = grab(cam)\nlast_chunks(cam)\n# Dict{Symbol, Any}(:ChunkTimestamp => 18530,\n#                   :ChunkExposureTime => 1.33e6,\n#                   :ChunkLineStatusAll => 0)\n\nStop chunks with disable_chunks! — turns off ChunkModeActive and clears the binding cache.","title":"Reading"},{"category":"section","location":"chunks_events/#How-the-decoder-finds-chunk-bytes","page":"Chunks and events","text":"Two paths, picked per binding at parse time:\n\nCanonical (<ChunkID> tagged): the producer reports the chunk layout via DSGetBufferChunkData, returning a list of (chunk_id, offset, length) triples for the current buffer. We match each triple to a binding's chunk_id and decode the bytes using the node type's codec (IntReg / FloatReg / StringReg / MaskedIntReg / Converter / ...).\nProducer-virtual (no <ChunkID>, but the chunk feature lives on a vendor-private virtual port): the producer routes register reads on that port to the most recent buffer's metadata. We just re-read the feature value through the nodemap after invalidating the cache so each frame gets fresh data.\n\nMost cameras use one mechanism or the other; some (mvBlueFOX3 + Balluff producer combinations) use the second exclusively. enable_chunks! registers bindings for both kinds; decode_chunks! picks the right path per binding.","title":"How the decoder finds chunk bytes"},{"category":"section","location":"chunks_events/#Chunks-during-streaming","page":"Chunks and events","text":"Chunks work transparently during stream when decode = true — the streaming loop calls the same decode pipeline that grab uses, so each DecodedFrame arrives with chunks already parsed and the camera's last_chunks updated.\n\nWhen decode = false (raw bytes path), the chunk fields aren't populated automatically — call decode_chunks! on the raw Frame yourself.\n\nSee API reference for full signatures of chunk_features, enable_chunks!, disable_chunks!, decode_chunks!, last_chunks, and ChunkBinding.\n\n","title":"Chunks during streaming"},{"category":"section","location":"chunks_events/#Events","page":"Chunks and events","text":"GenTL defines four event types; two of them (EVENT_FEATURE_INVALIDATE, EVENT_FEATURE_CHANGE) are about camera-side feature changes that the producer wants to push to the consumer. Many producers don't actually fire these; this section assumes they do.","title":"Events"},{"category":"section","location":"chunks_events/#Registering-listeners","page":"Chunks and events","text":"hd = on_feature_invalidate(cam) do event\n    @info \"camera invalidated\" feature=event.name kind=event.kind\nend\n\nOr filter by name:\n\non_feature_change(cam, \"ExposureTime\") do event\n    @info \"exposure changed to\" event.value\nend\n\non_feature_invalidate and on_feature_change lazily spawn a single background task per camera that loops over EventGetData with a finite timeout. Multiple listeners share the same pump.","title":"Registering listeners"},{"category":"section","location":"chunks_events/#Graceful-degradation","page":"Chunks and events","text":"Producers that don't support feature events return GC_ERR_NOT_AVAILABLE or GC_ERR_NOT_IMPLEMENTED from GCRegisterEvent. The library catches both, logs a single @warn, and returns a no-op ListenerHandle (id 0). User code stays portable: a listener that's never going to fire is silently ignored rather than turning into a hard error.\n\nhd = on_feature_invalidate(cam, callback)\n# WARN: producer does not support EVENT_FEATURE_INVALIDATE\n# hd === ListenerHandle(0)   — no-op handle\nremove_listener(cam, hd)        # also no-op, but legal","title":"Graceful degradation"},{"category":"section","location":"chunks_events/#Unregistering","page":"Chunks and events","text":"remove_listener(cam, hd)\n\nOr close the entire pump (rarely needed; close(cam) does it):\n\nclose_event_pump!(cam)","title":"Unregistering"},{"category":"section","location":"chunks_events/#What-gets-dispatched","page":"Chunks and events","text":"A FeatureEvent record carries kind (:invalidate or :change), the feature name, an optional value (for :change only), and the wall-clock receipt time.\n\nFor :invalidate events, the library also clears the matching node's cache before firing the listener — so a listener that calls get_feature(cam, event.name) re-reads from the device.\n\nSee API reference for full signatures of on_feature_invalidate, on_feature_change, remove_listener, and close_event_pump!.","title":"What gets dispatched"},{"category":"section","location":"hotplug/#Hot-plug-detection","page":"Hot-plug detection","text":"Two related needs:\n\nOne-shot enumeration across producers — \"show me every camera on this machine, no matter which vendor\". Use list_all_cameras.\nLive device-list monitoring — \"tell me when somebody plugs or unplugs a camera\". Use watch_devices (single producer) or watch_all_producers (every installed .cti).\n\nBoth are polling-based on purpose: GenTL standardises an interface- update event (TLUpdateInterfaceList-driven), but very few producers actually fire it. A 2-second poll is reliable across vendors, cheap, and good enough for a UI device list.","title":"Hot-plug detection"},{"category":"section","location":"hotplug/#list*all*cameras","page":"Hot-plug detection","text":"list_all_cameras()\n# Vector{NamedTuple{(:producer_path, :interface, :device), ...}}:\n#   (producer_path = \".../Balluff/.../mvGenTLProducer.cti\",\n#    interface     = InterfaceInfo(id=\"...USB...\", tl_type=\"U3V\"),\n#    device        = DeviceInfo(id=\"VID164C_PID5533_F0700035\",\n#                               vendor=\"MATRIX VISION GmbH\",\n#                               model=\"mvBlueFOX3-1013C\", ...))\n\nThis walks every .cti returned by list_producers, opens each transiently, enumerates its visible cameras, and returns every device tagged with the producer that reported it. Producers that fail to load are skipped with @warn; they don't break the walk.\n\nUseful at app startup to discover what's available. The returned tuples are pickled enough that you can serialize them, hand to a GUI, etc.","title":"listallcameras"},{"category":"section","location":"hotplug/#watch_devices","page":"Hot-plug detection","text":"watch_devices(producer; interval = 2.0, timeout_ms = 500) do channel\n    for event in channel\n        if event.kind === :added\n            println(\"plugged in: \", event.device.model)\n        else\n            println(\"unplugged: \", event.device.model)\n        end\n    end\nend\n\nThe do-block variant guarantees stop_watch! runs on exit. Outside a do-block:\n\nch = watch_devices(producer; interval = 2.0)\n# ... later ...\nstop_watch!(ch)","title":"watch_devices"},{"category":"section","location":"hotplug/#What-you-get","page":"Hot-plug detection","text":"The watcher emits one :added event per device that appears (newly visible since the previous poll) and one :removed event per device that disappears. On the very first poll, every currently-attached device counts as \"newly visible\" — so you'll see one :added per existing camera at start-up.\n\nThe channel has a small bounded capacity (default 32). If your consumer is slow and the queue fills, the oldest event is dropped to make room — consistent with the streaming-API's DROP_OLDEST policy. Don't rely on observing every event; rely on a periodic re-enumeration via list_all_cameras for ground truth.","title":"What you get"},{"category":"section","location":"hotplug/#Polling-cadence","page":"Hot-plug detection","text":"The interval arg is the wall time between full enumerations. Two seconds is a reasonable default — short enough to feel responsive, long enough not to hammer the producer. For latency-sensitive use cases you can drop to 0.5 s; below that some producers stutter or the device list flickers under load.\n\ntimeout_ms is the per-call cap on tl_update_interface_list / if_update_device_list. 500 ms is plenty even on slow USB / GigE.","title":"Polling cadence"},{"category":"section","location":"hotplug/#watch*all*producers","page":"Hot-plug detection","text":"Same idea, fanning out across every installed producer:\n\nch = watch_all_producers(; interval = 2.0)\nfor event in ch\n    println(event.kind, \" \", event.device.model,\n            \" under \", basename(event.producer_path))\nend\nstop_watch!(ch)\n\nOne polling task per producer, all funnelling into a single channel. stop_watch! shuts every task down and closes the channel.\n\nSee API reference for full signatures of list_all_cameras, watch_devices, watch_all_producers, stop_watch!, and DeviceEvent.","title":"watchallproducers"},{"category":"section","location":"quickstart/#Quickstart","page":"Quickstart","text":"This page walks through everything from a fresh Julia REPL to a displayed image. About 5 minutes if a camera and producer are already installed; about 30 minutes if you're starting from scratch.","title":"Quickstart"},{"category":"section","location":"quickstart/#1.-Install-a-GenTL-Producer","page":"Quickstart","text":"GenICam.jl is a consumer. It needs a producer — a vendor-supplied DLL (file extension .cti) that talks to your camera over its physical transport (GigE Vision, USB3 Vision, Camera Link, CoaXPress, ...). You get one from any vendor's SDK; the producers below have been tested with this package:\n\nProducer Source Targets\nBalluff mvGenTLProducer.cti Balluff Impact Acquire GigE Vision, USB3 Vision\nMATRIX VISION mvIMPACT producer MATRIX VISION website GigE / USB3 Vision\nBasler GenTL producer Basler pylon SDK Basler cameras\nStemmer Common Vision Blox producer Stemmer Imaging various\n\nMost installers set the GENICAM_GENTL64_PATH environment variable for you. Verify with:\n\nENV[\"GENICAM_GENTL64_PATH\"]\n# \"C:\\\\Program Files\\\\Balluff\\\\ImpactAcquire\\\\bin\\\\x64\"\n\nIf it's empty or missing, point it at the directory containing the .cti file. Multiple paths are separated by ; on Windows or : on Linux/macOS.","title":"1. Install a GenTL Producer"},{"category":"section","location":"quickstart/#2.-Install-GenICam.jl","page":"Quickstart","text":"using Pkg\nPkg.add(url = \"https://github.com/schrpe/GenICam.jl\")\n\n(Once the package is registered the URL form will be unnecessary.)","title":"2. Install GenICam.jl"},{"category":"section","location":"quickstart/#3.-Find-a-producer-and-a-camera","page":"Quickstart","text":"using GenICam, GenICam.GenTL\n\nlist_producers()\n# Vector{String}:\n#   \"C:\\\\Program Files\\\\Balluff\\\\ImpactAcquire\\\\bin\\\\x64\\\\mvGenTLProducer.cti\"\n#   \"C:\\\\Program Files\\\\Balluff\\\\ImpactAcquire\\\\bin\\\\x64\\\\mvGenTLProducer.PCIe.cti\"\n\nlist_all_cameras()\n# 1-element Vector{...}:\n#   (producer_path = \".../mvGenTLProducer.cti\",\n#    interface     = InterfaceInfo(...U3V),\n#    device        = DeviceInfo(\"F0700035\", \"MATRIX VISION GmbH\",\n#                               \"mvBlueFOX3-1013C\", ...))\n\nlist_all_cameras walks every installed producer; useful when you're not sure which one your camera lives behind. For a single known producer use list_cameras:\n\napi = load_producer(first(list_producers()))\nproducer = Producer(api)\nlist_cameras(producer)","title":"3. Find a producer and a camera"},{"category":"section","location":"quickstart/#4.-Open-the-camera","page":"Quickstart","text":"info = first(list_cameras(producer))\ncam = open_camera(producer, info.interface, info.device)\n# Camera(F0700035, 1184 nodes)\n\nopen_camera does several things in one call:\n\nOpens the camera's transport (GigE / USB3 channel).\nOpens a data stream on it.\nDownloads the GenApi XML from the camera (often ZIP-compressed, several hundred KB).\nParses the XML into a Nodemap — a graph of feature nodes that describes every register, formula, and selector chain.\nReturns a Camera bundling all of the above.","title":"4. Open the camera"},{"category":"section","location":"quickstart/#5.-Read-and-write-features","page":"Quickstart","text":"The simplest way is property syntax. Tab-completion works in the REPL:\n\ncam.Width = 640\ncam.Height = 480\ncam.PixelFormat = \"Mono8\"\ncam.ExposureTime           # 20000.0  (microseconds)\ncam.DeviceVendorName       # \"MATRIX VISION GmbH\"\n\nBehind the scenes this calls set_feature! / get_feature. You can also call those directly:\n\nset_feature!(cam, :Width, 640)\nget_feature(cam, \"Width\")\nexecute_command!(cam, :TriggerSoftware)\n\nBoth spellings work, both spellings are supported. Property syntax is shorter; the explicit calls are easier to grep for in scripts and skip property-name validation (no surprise when a feature name happens to match a Julia field name).","title":"5. Read and write features"},{"category":"section","location":"quickstart/#6.-Set-up-an-acquisition","page":"Quickstart","text":"The seven SFNC mandatories — Width, Height, PixelFormat, PayloadSize, AcquisitionMode, AcquisitionStart, AcquisitionStop — are enough for a basic capture. Most cameras also expose ExposureTime, Gain, and trigger-related features; the SFNC convenience helpers configure them in the right order:\n\nset_aoi!(cam; x = 0, y = 0, width = 640, height = 480)\nset_exposure!(cam, 10_000.0)        # microseconds\nset_gain!(cam, 0.0)                  # decibels\ndisable_trigger!(cam)                # free-running\ncam.PixelFormat = \"Mono8\"\n\nSee Features for the full feature-access reference.","title":"6. Set up an acquisition"},{"category":"section","location":"quickstart/#7.-Grab-one-frame","page":"Quickstart","text":"frame = grab(cam; timeout_ms = 1000)\n# DecodedFrame{Matrix{Gray{N0f8}}}(Mono8, 480x640)\n\nframe.image     # Matrix{Gray{N0f8}} of size (480, 640)\nframe.pixel_format    # :Mono8\n\ngrab returns a DecodedFrame — a thin wrapper carrying the typed Julia array plus metadata (pixel-format symbol, CFA pattern for Bayer images). For raw bytes use grab_raw.\n\nThe matrix is column-major and indexed (row, col) — frame.image[1, 1] is the top-left pixel.","title":"7. Grab one frame"},{"category":"section","location":"quickstart/#8.-Stream-continuously","page":"Quickstart","text":"stream(cam; num_buffers = 8) do channel\n    for img in Iterators.take(channel, 100)\n        process(img.image)\n    end\nend\n\nstream spawns a background task that pushes decoded frames into a Channel. The do-block guarantees stop_stream is called even if your loop throws.\n\nSee Streaming for backpressure policies, statistics, and the explicit start_stream / stop_stream form.","title":"8. Stream continuously"},{"category":"section","location":"quickstart/#9.-Close-cleanly","page":"Quickstart","text":"close(cam)         # tears down stream / event pump / data stream / interface\nclose(producer)\nclose(api)\n\nFinalizers also clean up if you forget — but explicit close is recommended in long-running programs so the camera is released promptly.","title":"9. Close cleanly"},{"category":"section","location":"quickstart/#10.-Putting-it-all-together","page":"Quickstart","text":"A complete script:\n\nusing GenICam, GenICam.GenTL\n\napi      = load_producer(first(list_producers()))\nproducer = Producer(api)\ninfo     = first(list_cameras(producer))\ncam      = open_camera(producer, info.interface, info.device)\n\ntry\n    set_aoi!(cam; width = 640, height = 480)\n    cam.PixelFormat = \"Mono8\"\n    set_exposure!(cam, 5000.0)\n\n    stream(cam) do channel\n        for (i, img) in enumerate(Iterators.take(channel, 30))\n            stats = (mean = sum(img.image) / length(img.image),\n                     min  = minimum(img.image),\n                     max  = maximum(img.image))\n            @info \"frame\" i stats\n        end\n    end\nfinally\n    close(cam); close(producer); close(api)\nend\n\nThat's everything you need for free-running 30-frame acquisition with proper cleanup. From here, browse the topical pages to dig deeper into the parts you care about.","title":"10. Putting it all together"},{"category":"section","location":"#GenICam.jl","page":"Home","text":"Pure-Julia implementation of the EMVA GenICam standard family — device discovery, configuration, image acquisition, and per-frame metadata for any vendor's machine-vision camera that ships a GenTL producer DLL.\n\n \nStandard GenTL 1.5, GenApi 1.1, SFNC, PFNC\nPlatforms Windows x64, Linux x64, macOS x64\nJulia 1.12+\nLicense MIT","title":"GenICam.jl"},{"category":"section","location":"#What-is-GenICam?","page":"Home","text":"GenICam is the open standard for industrial camera control, maintained by the EMVA. It defines four pieces that fit together at runtime:\n\nGenTL — a C ABI exposed by vendor-supplied .cti \"producer\" DLLs. Handles device enumeration, register I/O, and image transport.\nGenApi — an XML node-map embedded in each camera that describes the camera's features (Width, ExposureTime, Gain, ...) and how each one maps to a register or a computed expression.\nSFNC (Standard Features Naming Convention) — a vocabulary so every vendor exposes \"Width\", \"ExposureTime\", \"TriggerMode\" with the same names and units.\nPFNC (Pixel Format Naming Convention) — a numeric registry of pixel-format codes that buffers carry in their metadata.\n\nGenICam.jl reimplements the consumer side of all four in pure Julia — no GenApi C++ runtime, no vendor SDK linkage. Any producer that follows the GenTL ABI works.\n\nFor a deeper introduction to the standard's vocabulary (producers, handles, the node-map, formula evaluator, the acquisition lifecycle) see Concepts.","title":"What is GenICam?"},{"category":"section","location":"#Quick-example","page":"Home","text":"using GenICam, GenICam.GenTL\n\n# Find a vendor producer DLL via $GENICAM_GENTL64_PATH and load it.\napi = load_producer(first(list_producers()))\nproducer = Producer(api)\n\n# Pick the first visible camera, open it.\ninfo = first(list_cameras(producer))\ncam = open_camera(producer, info.interface, info.device)\n\n# Configure: set AOI, pixel format, exposure.\nset_aoi!(cam; width = 640, height = 480)\ncam.PixelFormat = \"Mono8\"\nset_exposure!(cam, 5000.0)        # 5 ms\n\n# Grab one frame as a typed Julia matrix.\nframe = grab(cam)\n@show size(frame.image) eltype(frame.image) frame.pixel_format\n\nclose(cam); close(producer); close(api)\n\nFor continuous acquisition use the streaming API:\n\nstream(cam) do channel\n    for img in Iterators.take(channel, 100)\n        process(img.image)\n    end\nend","title":"Quick example"},{"category":"section","location":"#Architecture","page":"Home","text":"GenICam (top-level package)\n│\n├── GenTL ─────────────  Pure-Julia binding to GenTL_v1_5.h\n│   ├── ProducerAPI      (.cti loaded via Libdl + dlsym)\n│   ├── Producer         TL_HANDLE + lifecycle\n│   ├── Interface        IF_HANDLE\n│   ├── Device           DEV_HANDLE\n│   ├── DataStream       DS_HANDLE\n│   └── Acquisition      buffer pool + EVENT_NEW_BUFFER loop\n│\n├── GenApi ────────────  Camera XML → Node graph\n│   ├── 15 concrete node types\n│   ├── 3-pass parser    (forward refs, invalidator wiring, formula compile)\n│   ├── SwissKnife       full Pratt-parsed expression evaluator\n│   ├── Cache + invalidators\n│   └── Streamable save/load\n│\n├── PixelFormats ──────  PFNC decoders → typed Matrices\n│   └── Mono / Bayer / RGB / BGR / RGBa / BGRa / YUV at 8 / 10 / 12 / 14 / 16 bits\n│\n├── Camera (top-level)   High-level API: `cam.Width = 640`, `grab(cam)`\n├── streaming.jl         `start_stream` / `stream do`\n├── chunks.jl            per-frame metadata\n├── events.jl            feature-invalidate / feature-change listeners\n├── sfnc.jl              `set_aoi!` / `set_trigger!` / `set_exposure!` / ...\n└── hotplug.jl           `list_all_cameras` / `watch_devices`","title":"Architecture"},{"category":"section","location":"#Status","page":"Home","text":"Area Status\nGenTL consumer (full GenTL_v1_5.h surface) live-tested\nGenApi node-map (15 node types, all SwissKnife operators) 99.85% feature read on a real camera\n7 SFNC mandatories live-tested\nPixel formats (Mono/Bayer/RGB/BGR/YUV) unit-tested + live-tested\nStreaming with backpressure live-tested\nChunks (canonical + producer-virtual) live-tested\nFeature events (with graceful degradation) API live-tested\nHot-plug detection live-tested\nCross-platform (Windows / Linux / macOS) Windows live, Linux/macOS via CI\nDocumentation this site","title":"Status"},{"category":"section","location":"#Documentation-map","page":"Home","text":"Background — what is GenICam?\n\nConcepts — the four GenICam pieces explained (Producer/Consumer model, GenTL handle hierarchy, GenApi node-map, SFNC, PFNC, full acquisition lifecycle).\nThe SwissKnife formula language — operator precedence, built-in functions, FROM / TO convention, real examples from camera XMLs.\n\nPractical guides\n\nQuickstart — installation, environment setup, first grab.\nFeatures — cam.X syntax, SFNC mandatories, categories, selectors, visibility, predicates, caching.\nPixel formats — supported codes, return types, PFNC bit-layout, Bayer policy.\nStreaming — continuous acquisition, drop policies, statistics.\nChunks and events — per-frame metadata, feature listeners.\nHot-plug detection — multi-producer enumeration, device-watch channels.\n\nReference\n\nVendor notes — known camera quirks (MATRIX VISION, Balluff, ...).\nAPI reference — every exported name with full signature.","title":"Documentation map"},{"category":"section","location":"#Comparison-with-other-GenICam-libraries","page":"Home","text":" GenICam.jl Aravis (GLib/C) Harvesters (Python) Pylon (Basler-only)\nLanguage pure Julia C Python (binds GenApi C++) C++ / .NET wrapper\nGenApi own pure-Julia parser + evaluator own pure-C reimplementation binds the EMVA reference C++ Basler's vendor lock\nVendor DLL needed .cti producer (any vendor) producer not used (Aravis is itself a producer) .cti producer Basler-only\nCross-platform Windows / Linux / macOS Linux / Windows Windows / Linux Windows / Linux","title":"Comparison with other GenICam libraries"},{"category":"module","location":"#GenICam.GenICam","page":"Home","text":"GenICam\n\nPure-Julia implementation of the EMVA GenICam standard family.\n\nThe package implements the consumer side of all four GenICam pieces:\n\nGenTL — binding for the GenTL Standard 1.5 Producer C API; loads any vendor .cti DLL via Libdl.\nGenApi — full node-map: 15 concrete node types, 3-pass parser with forward-reference / invalidator wiring, SwissKnife expression evaluator, caching, <pIsAvailable> / <pIsImplemented> predicates, <Streamable> save / load.\nPixelFormats — PFNC decoders to typed Julia matrices (Gray{N0fX} / RGB{N0fX} / BGR{N0fX} / ...).\nCamera — the high-level handle: cam.Width = 640, grab(cam), stream(cam) do ch ... end, plus SFNC convenience helpers and chunk / event / hot-plug layers.\n\n\n\n\n\n","title":"GenICam.GenICam"}]
}
