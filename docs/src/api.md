```@meta
CurrentModule = GenICam
```

# API reference

Auto-generated from doc-strings on every exported name. Topical
overviews live on the dedicated pages — this page is the comprehensive
list.

## Top-level package (`GenICam`)

```@autodocs
Modules = [GenICam]
Order   = [:type, :function, :macro, :constant]
```

## Pixel formats (`GenICam.PixelFormats`)

```@autodocs
Modules = [GenICam.PixelFormats]
Order   = [:type, :function, :macro, :constant]
```

## GenApi (`GenICam.GenApi`)

```@autodocs
Modules = [GenICam.GenApi]
Order   = [:type, :function, :macro, :constant]
```

## GenTL (`GenICam.GenTL`)

```@autodocs
Modules = [GenICam.GenTL]
Order   = [:type, :function, :macro, :constant]
```
